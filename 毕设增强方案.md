# 软件工程毕设增强方案

> 基于现有蛋白Binder设计系统，从软件工程角度补充功能模块
> 不涉及模型微调或算法修改，仅增加软工维度的工程化能力

---

## 一、现有系统评估

### 已具备的软工要素
- ✅ 前后端分离架构（React + FastAPI）
- ✅ 状态管理（Zustand）
- ✅ 分步工作流（RFD3 → MPNN → RF3）
- ✅ 三级回退机制（API → CLI → Mock）
- ✅ Docker 容器化部署
- ✅ Git 版本管理

### 缺失的软工要素（毕设答辩高频考点）
- ❌ 单元测试 / 集成测试
- ❌ 用户认证与权限管理
- ❌ 实验记录持久化（数据库）
- ❌ API 文档自动生成
- ❌ 日志系统与运行监控
- ❌ CI/CD 流水线
- ❌ UML 设计文档
- ❌ 错误处理与异常恢复

---

## 二、推荐新增功能（按优先级排序）

### 2.1 实验记录管理系统（⭐⭐⭐ 最高优先级）

**答辩价值**：体现数据持久化设计、CRUD 全流程、数据库选型

**功能描述**：
- 每次设计任务自动保存为一条实验记录
- 记录包含：输入参数、模型配置、输出结果、耗时、状态
- 支持实验列表查看、搜索、筛选、对比
- 支持导出实验报告（PDF/Excel）

**技术方案**：
```
SQLite（轻量）或 PostgreSQL（生产级）
├── experiments 表
│   ├── id, name, created_at, status
│   ├── input_pdb (TEXT), target (TEXT), hotspots (JSON)
│   ├── rfd3_config (JSON), mpnn_config (JSON), rf3_config (JSON)
│   ├── rfd3_results (JSON), mpnn_results (JSON), rf3_results (JSON)
│   ├── duration_seconds, gpu_info
│   └── user_id (FK)
├── experiment_designs 表
│   ├── id, experiment_id (FK)
│   ├── design_name, sequence, pdb_content
│   ├── plddt, rmsd, ranking_score
│   └── passed_validation
└── users 表
    ├── id, username, email, role
    └── created_at, last_login
```

**新增 API**：
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/experiments` | GET | 实验列表（分页+筛选） |
| `/api/experiments/{id}` | GET | 实验详情 |
| `/api/experiments` | POST | 创建实验记录 |
| `/api/experiments/{id}` | DELETE | 删除实验 |
| `/api/experiments/{id}/export` | GET | 导出实验报告 |
| `/api/experiments/compare` | POST | 实验对比 |

---

### 2.2 用户认证与权限管理（⭐⭐⭐）

**答辩价值**：体现安全设计、RBAC 权限模型、JWT 认证

**功能描述**：
- 用户注册/登录（JWT Token）
- 角色权限：管理员（全权限）、研究员（设计+查看）、访客（仅查看）
- 操作审计日志

**技术方案**：
```python
# JWT 认证中间件
from fastapi import Depends, HTTPException
from fastapi.security import OAuth2PasswordBearer

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/auth/login")

async def get_current_user(token: str = Depends(oauth2_scheme)):
    payload = jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
    user = get_user(payload["sub"])
    if not user:
        raise HTTPException(401, "无效凭证")
    return user

# RBAC 权限装饰器
def require_role(role: str):
    def decorator(func):
        async def wrapper(*args, current_user=Depends(get_current_user), **kwargs):
            if current_user.role not in ALLOWED_ROLES[role]:
                raise HTTPException(403, "权限不足")
            return await func(*args, current_user=current_user, **kwargs)
        return wrapper
    return decorator
```

**新增 API**：
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/login` | POST | 用户登录（返回JWT） |
| `/api/auth/me` | GET | 当前用户信息 |
| `/api/users` | GET | 用户列表（管理员） |

---

### 2.3 单元测试与集成测试（⭐⭐⭐）

**答辩价值**：体现软件质量保证、测试驱动开发、覆盖率

**测试分层**：

```
tests/
├── unit/                          # 单元测试
│   ├── test_rfd3_runner.py        # RFD3 参数构建、PDB 解析
│   ├── test_mpnn_runner.py        # MPNN 序列提取、配置验证
│   ├── test_rf3_runner.py         # RF3 RMSD 计算、指标提取
│   ├── test_hotspot_predictor.py  # 热点预测 Top-K、规则评分
│   ├── test_pdb_parser.py         # PDB 解析边界情况
│   └── test_api_models.py         # Pydantic 模型验证
├── integration/                   # 集成测试
│   ├── test_pipeline.py           # RFD3→MPNN→RF3 全流程
│   ├── test_api_endpoints.py      # API 端点测试
│   └── test_database.py           # 数据库 CRUD
└── conftest.py                    # 公共 fixtures
```

**关键测试用例**：
```python
# test_rfd3_runner.py
def test_build_binder_contig_single_chain():
    runner = RFD3Runner()
    result = runner._build_binder_contig({"A": (65, 135)}, 80)
    assert result == "80,/0,A65-135"

def test_build_binder_contig_multi_chain():
    runner = RFD3Runner()
    result = runner._build_binder_contig({"A": (1, 50), "B": (65, 135)}, 80)
    assert "80" in result
    assert "A1-50" in result
    assert "B65-135" in result

def test_rmsd_identical_structures():
    runner = RF3Runner()
    pdb = read_test_pdb("1abc.pdb")
    rmsd = runner._calculate_rmsd_pdb(pdb, pdb)
    assert rmsd < 0.01  # 相同结构RMSD应接近0

def test_hotspot_top_k():
    predictor = HotspotPredictor(top_k=5)
    result = predictor.predict_hotspots(test_pdb, top_k=3)
    assert result["num_hotspots"] == 3
    assert len(result["hotspots_detail"]) == 3

# test_api_endpoints.py
def test_predict_hotspot_api(client):
    resp = client.post("/api/predict-hotspot", json={"pdb_content": test_pdb})
    assert resp.status_code == 200
    data = resp.json()
    assert "hotspots" in data
    assert data["num_hotspots"] > 0
```

**运行与覆盖率**：
```bash
pytest tests/ -v --cov=utils --cov=backend --cov-report=html
# 目标覆盖率: > 60%
```

---

### 2.4 API 文档自动生成（⭐⭐）

**答辩价值**：体现接口规范化、Swagger/OpenAPI 标准

**实现方式**：FastAPI 自带 Swagger UI，只需补充描述

```python
# design.py
@router.post(
    "/run-rfd3",
    summary="RFD3 骨架生成",
    description="""
    使用 RFDiffusion3 生成蛋白质 Binder 骨架结构。

    **输入**：目标蛋白 PDB + 热点残基 + Binder 长度
    **输出**：多个骨架结构（PDB格式）+ pLDDT 置信度

    **回退机制**：
    1. foundry Python API（GPU推理）
    2. Mock 模式（随机生成）
    """,
    response_description="生成的骨架结构列表",
    responses={
        200: {"description": "成功生成骨架"},
        500: {"description": "模型推理失败"},
    }
)
async def run_rfd3(req: RFD3Request):
    ...
```

访问地址：`http://localhost:8000/docs`

---

### 2.5 日志系统与运行监控（⭐⭐）

**答辩价值**：体现运维能力、可观测性设计

**功能描述**：
- 结构化日志（JSON格式）
- 任务耗时统计
- GPU 显存监控
- 错误追踪与告警

**技术方案**：
```python
# logger.py
import logging
import json
import time
from functools import wraps

class StructuredLogger:
    def __init__(self, name):
        self.logger = logging.getLogger(name)
        handler = logging.StreamHandler()
        handler.setFormatter(logging.Formatter('%(message)s'))
        self.logger.addHandler(handler)
        self.logger.setLevel(logging.INFO)

    def info(self, msg, **kwargs):
        self.logger.info(json.dumps({"level": "info", "msg": msg, **kwargs}, default=str))

    def error(self, msg, **kwargs):
        self.logger.error(json.dumps({"level": "error", "msg": msg, **kwargs}, default=str))

def track_performance(func):
    @wraps(func)
    async def wrapper(*args, **kwargs):
        start = time.time()
        try:
            result = await func(*args, **kwargs)
            duration = time.time() - start
            logger.info("task_completed", function=func.__name__, duration=f"{duration:.2f}s", status="success")
            return result
        except Exception as e:
            duration = time.time() - start
            logger.error("task_failed", function=func.__name__, duration=f"{duration:.2f}s", error=str(e))
            raise
    return wrapper
```

**新增监控 API**：
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/monitor/status` | GET | 系统状态（GPU/内存/模型） |
| `/api/monitor/tasks` | GET | 当前运行任务 |
| `/api/monitor/history` | GET | 历史任务统计 |

---

### 2.6 CI/CD 流水线（⭐⭐）

**答辩价值**：体现 DevOps 实践、自动化质量保障

**GitHub Actions 配置**：
```yaml
# .github/workflows/ci.yml
name: CI/CD Pipeline

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: pytest tests/ -v --cov

  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: cd binder-design-system/frontend && npm install
      - run: cd binder-design-system/frontend && npx tsc --noEmit
      - run: cd binder-design-system/frontend && npm run build

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install ruff
      - run: ruff check binder-design-system/
```

---

### 2.7 参数配置管理（⭐）

**答辩价值**：体现配置外部化、环境隔离

**功能描述**：
- 前端可视化参数编辑器
- 预设模板（快速启动常用配置）
- 参数合法性校验
- 配置导入/导出（JSON）

**预设模板**：
```json
{
  "presets": {
    "快速验证": {
      "binder_length": 60,
      "diffusion_batch_size": 1,
      "n_batches": 1,
      "mpnn_batch_size": 3,
      "description": "快速验证可行性，1个骨架+3条序列"
    },
    "标准设计": {
      "binder_length": 80,
      "diffusion_batch_size": 2,
      "n_batches": 2,
      "mpnn_batch_size": 10,
      "description": "标准设计流程，4个骨架+10条序列"
    },
    "深度筛选": {
      "binder_length": 100,
      "diffusion_batch_size": 4,
      "n_batches": 3,
      "mpnn_batch_size": 20,
      "description": "深度筛选，12个骨架+20条序列"
    }
  }
}
```

---

### 2.8 实验对比与可视化分析（⭐）

**答辩价值**：体现数据分析能力、可视化设计

**功能描述**：
- 多实验横向对比（RMSD、pLDDT、序列相似性）
- 设计结果统计图表（matplotlib/echarts）
- 序列多序列比对（MSA）可视化
- 结构叠加对比视图

---

## 三、UML 设计文档（毕设论文必备）

### 3.1 系统用例图

```
角色：研究员、访客、管理员

研究员：
  - 上传蛋白结构
  - 预测热点残基
  - 运行RFD3骨架生成
  - 运行MPNN序列设计
  - 运行RF3结构验证
  - 查看/导出实验结果
  - 对比历史实验

访客：
  - 查看实验列表
  - 查看公开实验详情

管理员：
  - 用户管理
  - 系统监控
  - 模型状态管理
```

### 3.2 系统架构图

```
┌─────────────────────────────────────────────────┐
│                   前端 (React)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 结构查看 │ │ 序列编辑 │ │ 结果可视化+对比  │ │
│  └────┬─────┘ └────┬─────┘ └────────┬─────────┘ │
│       └────────────┼───────────────┘            │
│              HTTP API (Axios)                    │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────┼────────────────────────────┐
│              后端 (FastAPI)                       │
│  ┌─────────┐ ┌─────────┐ ┌──────────────────┐  │
│  │ 认证    │ │ 路由    │ │ 业务逻辑        │  │
│  │ (JWT)   │ │ (Router)│ │ (Runners)       │  │
│  └────┬────┘ └────┬────┘ └────────┬─────────┘  │
│       └───────────┼───────────────┘            │
│  ┌─────────┐ ┌────┴─────┐ ┌──────────────────┐ │
│  │ 日志    │ │ 数据库   │ │ 模型引擎        │ │
│  │ (JSON)  │ │ (SQLite) │ │ (RFD3/MPNN/RF3) │ │
│  └─────────┘ └──────────┘ └──────────────────┘ │
└─────────────────────────────────────────────────┘
```

### 3.3 核心时序图：蛋白Binder设计流程

```
研究员          前端            后端            RFD3           MPNN           RF3
  │              │              │              │              │              │
  │──上传PDB──→  │              │              │              │              │
  │              │──解析结构──→ │              │              │              │
  │              │←─链信息──── │              │              │              │
  │              │              │              │              │              │
  │──预测热点──→ │              │              │              │              │
  │              │──/predict──→ │              │              │              │
  │              │              │──ESM-2+GAT─→ │              │              │
  │              │              │←─热点列表─── │              │              │
  │              │←─Top-5───── │              │              │              │
  │              │              │              │              │              │
  │──RFD3生成──→ │              │              │              │              │
  │              │──/run-rfd3→ │              │              │              │
  │              │              │──run_rfd3──→ │              │              │
  │              │              │              │──GPU推理──→  │              │
  │              │              │              │←─骨架列表──  │              │
  │              │              │←─designs──── │              │              │
  │              │←─4个骨架─── │              │              │              │
  │              │              │              │              │              │
  │──送入MPNN──→ │              │              │              │              │
  │              │──/run-mpnn→ │              │              │              │
  │              │              │──run_mpnn───┼──────────────→│              │
  │              │              │              │              │──10条序列──→ │
  │              │              │←─sequences──┼────────────── │              │
  │              │←─10条序列── │              │              │              │
  │              │              │              │              │              │
  │──送入RF3───→ │              │              │              │              │
  │              │──/run-rf3──→ │              │              │              │
  │              │              │──run_rf3────┼──────────────┼──────────────→│
  │              │              │              │              │              │──验证──→
  │              │              │←─RMSD+指标─┼──────────────┼────────────── │
  │              │←─验证结果── │              │              │              │
  │←─查看结果── │              │              │              │              │
```

### 3.4 类图：核心业务模型

```
┌──────────────────┐     ┌──────────────────┐
│   Experiment     │     │   DesignResult   │
├──────────────────┤     ├──────────────────┤
│ id: str          │     │ id: str          │
│ name: str        │────→│ experiment_id: FK│
│ status: str      │     │ design_name: str │
│ created_at: dt   │     │ sequence: str    │
│ input_pdb: str   │     │ pdb_content: str │
│ rfd3_config: JSON│     │ plddt: float     │
│ mpnn_config: JSON│     │ rmsd: float      │
│ rf3_config: JSON │     │ ranking_score: f │
│ duration: float  │     │ passed: bool     │
│ user_id: FK      │     └──────────────────┘
└──────────────────┘
        │
        │ 1:N
        ▼
┌──────────────────┐     ┌──────────────────┐
│   User           │     │   AuditLog       │
├──────────────────┤     ├──────────────────┤
│ id: str          │     │ id: str          │
│ username: str    │────→│ user_id: FK      │
│ email: str       │     │ action: str      │
│ role: str        │     │ target: str      │
│ created_at: dt   │     │ timestamp: dt    │
└──────────────────┘     │ detail: JSON     │
                         └──────────────────┘
```

---

## 四、实施优先级与工作量

| 序号 | 功能 | 优先级 | 预估工作量 | 答辩加分点 |
|------|------|--------|-----------|-----------|
| 1 | 实验记录管理 | ⭐⭐⭐ | 2天 | 数据持久化、CRUD、导出 |
| 2 | 用户认证权限 | ⭐⭐⭐ | 1.5天 | JWT、RBAC、安全设计 |
| 3 | 单元测试 | ⭐⭐⭐ | 2天 | 测试覆盖率、质量保证 |
| 4 | API 文档 | ⭐⭐ | 0.5天 | 接口规范化 |
| 5 | 日志监控 | ⭐⭐ | 1天 | 可观测性、运维能力 |
| 6 | CI/CD | ⭐⭐ | 1天 | DevOps、自动化 |
| 7 | 参数配置管理 | ⭐ | 1天 | 配置外部化 |
| 8 | 实验对比分析 | ⭐ | 2天 | 数据分析、可视化 |

**建议实施顺序**：1 → 3 → 2 → 4 → 5 → 6 → 7 → 8

---

## 五、毕设论文结构建议

```
第1章 绪论
  1.1 研究背景与意义
  1.2 国内外研究现状（RFDiffusion、ProteinMPNN、RoseTTAFold）
  1.3 研究内容与论文结构

第2章 相关技术
  2.1 深度学习与蛋白质结构预测
  2.2 前后端分离架构
  2.3 React + FastAPI 技术栈
  2.4 Docker 容器化部署

第3章 系统需求分析
  3.1 功能需求（用例图）
  3.2 非功能需求（性能、安全、可用性）
  3.3 可行性分析

第4章 系统设计
  4.1 系统架构设计（架构图）
  4.2 数据库设计（ER图、表结构）
  4.3 接口设计（API文档）
  4.4 安全设计（JWT + RBAC）
  4.5 部署设计（Docker + GPU）

第5章 系统实现
  5.1 前端实现（结构查看、序列编辑、结果可视化）
  5.2 后端实现（分步工作流、三级回退、RMSD对比）
  5.3 模型集成（RFD3 + MPNN + RF3 + ESM-2 GAT）
  5.4 测试实现（单元测试、集成测试、覆盖率）

第6章 系统测试
  6.1 测试方案
  6.2 功能测试结果
  6.3 性能测试结果
  6.4 测试结论

第7章 总结与展望
```
