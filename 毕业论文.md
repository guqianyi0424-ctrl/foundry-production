# 基于深度学习的蛋白质Binder生成系统设计与实现

## 摘要

蛋白质-蛋白质相互作用（PPI）在生物体内几乎所有生命活动中都发挥着关键作用。设计能够与目标蛋白质特异性结合的Binder蛋白，是药物研发、疾病治疗和生物技术领域的重要方向。然而，传统的Binder设计依赖于实验筛选和专家经验，周期长、成本高且成功率低。近年来，深度学习技术在蛋白质结构预测与设计领域取得了突破性进展，为自动化Binder设计提供了新的可能。

本文设计与实现了一个基于深度学习的蛋白质Binder生成系统。该系统首先利用蛋白质语言模型ESM-2提取残基级别的序列-结构嵌入特征，结合图注意力网络（GAT）构建热点残基预测模型，在PPI-hotspotID数据集上进行了五折交叉验证和独立测试集评估。在独立测试集上，模型取得了ROC-AUC 0.7564、PR-AUC 0.1579、MCC 0.3023的性能，Top-3命中率38.3%、Top-5命中率46.7%、Top-10命中率51.7%。在此基础上，系统整合了RFdiffusion3（RFD3）扩散模型进行Binder骨架生成、ProteinMPNN进行序列设计、RoseTTAFold3（RF3）进行结构验证，实现了从目标蛋白输入到Binder候选输出的完整自动化流程。此外，本文基于React和FastAPI技术栈，设计并开发了DeepBinder Web应用，提供了直观的蛋白质结构可视化、热点残基标注和Binder设计交互界面。系统测试结果表明，该系统能够有效预测蛋白质结合界面上的关键残基，并完成端到端的Binder自动设计，为蛋白质药物开发提供了有价值的计算工具。

**关键词**：蛋白质Binder设计；热点残基预测；图注意力网络；蛋白质语言模型；深度学习

---

## Abstract

Protein-protein interactions (PPIs) play a crucial role in virtually all biological processes within living organisms. Designing binder proteins that specifically bind to target proteins is an important direction in drug development, disease treatment, and biotechnology. However, traditional binder design relies heavily on experimental screening and expert experience, characterized by long cycles, high costs, and low success rates. In recent years, deep learning technologies have achieved breakthrough progress in protein structure prediction and design, offering new possibilities for automated binder design.

This thesis presents the design and implementation of a deep learning-based protein binder generation system. The system first employs the protein language model ESM-2 to extract residue-level sequence-structure embedding features, combined with Graph Attention Networks (GAT) to construct a hotspot residue prediction model. The model was evaluated using five-fold cross-validation and independent test set assessment on the PPI-hotspotID dataset. On the independent test set, the model achieved ROC-AUC of 0.7564, PR-AUC of 0.1579, MCC of 0.3023, with Top-3 hit rate of 38.3%, Top-5 hit rate of 46.7%, and Top-10 hit rate of 51.7%. Building upon this, the system integrates the RFdiffusion3 (RFD3) diffusion model for binder backbone generation, ProteinMPNN for sequence design, and RoseTTAFold3 (RF3) for structure validation, enabling a complete automated workflow from target protein input to binder candidate output. Furthermore, a DeepBinder web application was designed and developed based on the React and FastAPI technology stack, providing intuitive protein structure visualization, hotspot residue annotation, and binder design interaction interfaces. System testing results demonstrate that this system can effectively predict critical residues on protein binding interfaces and complete end-to-end automated binder design, offering a valuable computational tool for protein drug development.

**Keywords**: Protein binder design; Hotspot residue prediction; Graph attention network; Protein language model; Deep learning

---

## 目录

- 第1章 绪论
- 第2章 相关技术
- 第3章 需求分析
- 第4章 系统设计
- 第5章 系统实现
- 第6章 系统测试
- 第7章 总结与展望
- 参考文献
- 致谢
- 附录

---

## 第1章 绪论

### 1.1 研究背景与意义

蛋白质是生命活动的主要执行者，几乎所有的细胞功能都离不开蛋白质的参与。在生物体内，蛋白质很少单独执行功能，而是通过与其他蛋白质发生物理相互作用来共同完成各项生命活动。蛋白质-蛋白质相互作用（Protein-Protein Interaction，PPI）在信号转导、免疫应答、基因表达调控、细胞周期控制等关键生物学过程中发挥着不可或缺的作用。PPI的异常与多种疾病的发生发展密切相关，包括癌症、神经退行性疾病、自身免疫病等。

在蛋白质相互作用界面上，存在着一类被称为"热点残基"（Hotspot Residues）的关键氨基酸残基。热点残基的概念最早由Clackson和Wells在1995年提出，他们在研究人生长激素与其受体的相互作用时发现，虽然结合界面上分布着大量残基，但结合自由能的主要贡献仅来源于少数几个关键残基。这些热点残基一旦发生突变（如丙氨酸扫描突变），会导致结合自由能发生显著变化（通常ΔΔG ≥ 2.0 kcal/mol）。因此，准确识别蛋白质相互作用界面上的热点残基，对于理解蛋白质相互作用机制、指导蛋白质药物设计和蛋白质工程改造具有重要的理论意义和应用价值。

Binder蛋白是指能够与目标蛋白质特异性结合的一类蛋白质分子。设计高亲和力、高特异性的Binder蛋白是蛋白质工程和药物开发领域的核心目标之一。传统的Binder设计方法主要依赖实验技术，如噬菌体展示、酵母双杂交、定向进化等，这些方法需要进行大规模的实验筛选，耗时费力且成本高昂。此外，传统实验方法的设计空间受到天然蛋白质序列和结构多样性的限制，难以实现对任意目标蛋白的精准Binder设计。

随着深度学习技术的快速发展，蛋白质科学领域正在经历一场革命性的变革。以AlphaFold2、RoseTTAFold为代表的结构预测模型实现了从氨基酸序列到三维结构的高精度预测，标志着计算结构生物学的重大突破。在蛋白质设计领域，基于扩散模型的RFdiffusion、基于图神经网络的ProteinMPNN等深度学习方法展现了强大的蛋白质骨架生成和序列设计能力。这些技术的涌现，使得开发自动化、智能化的Binder设计系统成为可能。

本研究旨在设计并实现一个基于深度学习的蛋白质Binder生成系统。该系统整合热点残基预测、Binder骨架生成、序列设计和结构验证等核心功能模块，通过Web应用提供便捷的交互界面，为研究人员提供一站式的Binder设计工具。本研究的实际应用价值体现在以下几个方面：（1）通过热点残基预测，帮助研究人员快速定位蛋白质结合界面上的关键残基，指导Binder设计的靶向策略；（2）通过自动化的Binder生成pipeline，大幅缩短Binder设计周期，降低实验成本；（3）通过可视化Web界面，降低使用门槛，使不具备深度学习专业背景的生物学家也能便捷地使用先进的计算设计工具。

### 1.2 国内外研究现状

#### 1.2.1 热点残基预测研究现状

热点残基的计算预测方法经历了从传统机器学习到深度学习的演进过程。在传统方法方面，Kortemme和Baker于2002年提出了基于物理能量函数的Roberta方法，通过模拟丙氨酸扫描突变来计算每个残基对结合自由能的贡献。Guerois等人开发了FoldX力场，能够快速评估突变对蛋白质稳定性和结合亲和力的影响。Darnell等人提出了基于知识势能的方法，利用已知结构的统计信息来预测热点残基。

在机器学习方法方面，Cho等人于2009年开发的KFC（Knowledge-based FADE and Contacts）方法结合了结构特征和序列特征进行热点预测。Lise等人开发的PredHS方法利用支持向量机对热点和非热点残基进行分类。Chen等人于2020年提出的PPI-hotspotID方法综合使用了传统机器学习算法和多种序列与结构特征，包括位置特异性打分矩阵（PSSM）、隐马尔可夫模型（HMM）谱、溶剂可及表面积（SASA）、序列保守性等特征，在公开数据集上取得了较好的预测性能。

近年来，深度学习方法在热点残基预测领域展现出显著优势。Li等人提出将蛋白质结构转化为图，利用图卷积网络进行界面残基的特征学习。DeepHoT方法利用三维卷积神经网络从蛋白质三维结构中直接学习空间特征。DeepHotResi方法首次将图注意力网络（GAT）应用于热点残基预测，通过在蛋白质图上进行消息传递来捕获残基间的复杂相互作用关系。

然而，现有方法仍面临若干挑战：（1）热点残基数据集规模较小，正负样本严重不平衡，不利于深度学习方法充分训练；（2）多数方法严重依赖人工设计的特征，特征工程复杂且可能遗漏关键信息；（3）不同方法在独立测试集上的泛化能力差异较大，实际应用可靠性有待提高。针对上述问题，本文提出了一种基于蛋白质语言模型ESM-2嵌入和图注意力网络的热点残基预测方法，利用预训练模型提供的丰富表示能力来弥补小样本训练的不足。

#### 1.2.2 蛋白质设计研究现状

蛋白质设计（Protein Design）是指根据预定的结构和功能目标，设计能够折叠成特定三维结构并执行特定功能的氨基酸序列。传统的蛋白质设计遵循"先骨架后序列"的范式：首先设计蛋白质骨架结构，然后寻找能够稳定折叠到该骨架的氨基酸序列。

在骨架生成方面，早期的Rosetta设计框架采用基于片段的组装策略，通过从已知蛋白质结构中获取结构片段并进行重组来生成新的骨架。近年来，深度学习模型在蛋白质骨架生成方面取得了重大突破。Watson等人于2023年提出的RFdiffusion方法，将去噪扩散概率模型（DDPM）应用于蛋白质骨架生成，通过在有条件的扩散过程中逐步去噪来生成与目标结构兼容的Binder骨架。该方法基于RoseTTAFold结构预测模型，利用其强大的结构表示能力来指导骨架生成过程，在多项实验验证中展现了优异的Binder设计成功率。RFD3（RFdiffusion3）是该方法的改进版本，在原有基础上进一步增强了对目标结合界面的条件控制能力。

在序列设计方面，Dauparas等人于2022年提出的ProteinMPNN方法将蛋白质结构视为图，其中节点代表氨基酸残基，边代表残基间的空间关系，通过消息传递机制学习给定骨架结构下最优的氨基酸序列。ProteinMPNN在天然蛋白质骨架的序列恢复率和设计序列的可表达性方面均显著优于传统的Rosetta固定骨架设计方法。LigandMPNN作为ProteinMPNN的扩展版本，进一步支持了小分子配体存在条件下的序列设计。

在结构验证方面，Baek等人于2024年提出的RoseTTAFold3（RF3）是该系列模型的最新版本，实现了蛋白质-蛋白质、蛋白质-核酸、蛋白质-小分子等多种生物分子复合物结构的高精度预测。RF3不仅能够评估设计序列是否能够折叠到预期的骨架结构，还能预测Binder与目标蛋白形成的复合物构象，为Binder设计的可靠性提供关键的结构验证信息。

#### 1.2.3 研究现状总结与本文思路

综合分析国内外研究现状，可以发现：（1）热点残基预测和蛋白质设计两个领域各自取得了显著进展，但将两者有机整合为完整Binder设计pipeline的研究尚不多见；（2）现有的蛋白质设计工具多以命令行或编程接口形式提供，缺乏面向非专业用户的友好交互界面；（3）预训练蛋白质语言模型与图神经网络的结合在蛋白质相关预测任务中展现出巨大潜力，但在热点残基预测中的应用仍有探索空间。

基于上述分析，本文提出了一种层次化的研究思路：首先，利用ESM-2预训练蛋白质语言模型提取富含序列和结构演化信息的残基嵌入特征，结合图注意力网络对蛋白质界面结构进行建模，实现高精度的热点残基预测；其次，将热点预测结果作为约束条件输入到Binder设计pipeline中，整合RFD3、ProteinMPNN和RF3三个核心模型，实现端到端的自动化Binder设计；最后，构建基于Web的交互式应用系统，为非专业用户提供直观便捷的操作界面。

### 1.3 论文研究内容

本文围绕基于深度学习的蛋白质Binder生成系统展开研究，主要研究内容包括以下几个方面：

（1）**数据预处理与特征提取**。针对PPI-hotspotID数据集进行标准化处理，包括PDB结构文件的解析、UniProt编号到PDB残基编号的映射、正负样本的标注（以ΔΔG ≥ 2.0 kcal/mol作为热点残基的判断阈值）。利用ESM-2（esm2_t33_650M_UR50D）预训练模型提取每个残基的1280维嵌入向量作为主要特征，同时支持PSSM、HMM等辅助特征的提取。

（2）**热点残基预测模型设计与实现**。设计并实现了基于图注意力网络的热点残基预测模型。该模型将每个蛋白质转化为图结构，以残基为节点、以空间距离为边构建规则，通过多层GAT进行节点特征学习，结合SE注意力机制和层次化池化策略聚合多尺度特征。针对样本不平衡问题，引入Focal Loss损失函数和标签平滑策略。实现了多种模型变体（GAT、GAT_v2、Ensemble、MLP）以支持对比实验和消融实验。

（3）**Binder设计pipeline集成**。将训练好的热点预测模型与RFD3骨架生成、ProteinMPNN序列设计、RF3结构验证三个模型进行整合，构建端到端的自动化Binder设计工作流。该pipeline以目标蛋白结构文件（PDB/CIF格式）为输入，经过热点残基预测筛选关键结合残基，利用RFD3扩散模型条件生成Binder骨架，通过ProteinMPNN设计氨基酸序列，最后使用RF3对设计结果进行结构验证和评估。

（4）**Web应用系统开发**。基于前后端分离架构，设计并实现了DeepBinder Web应用。前端采用React 18框架结合TypeScript和Zustand状态管理，并通过Molstar实现蛋白质三维结构的交互式可视化；后端采用FastAPI框架，提供RESTful API接口，包括用户认证、实验管理、文件上传、任务调度等功能模块。

（5）**系统测试与评估**。对热点预测模型进行了系统的实验评估，包括五折交叉验证、独立测试集评估、Top-K推荐指标分析、消融实验和基线方法对比。对Web应用进行了功能测试和界面测试，验证了系统的完整性和可用性。

### 1.4 论文组织结构

本论文共分为七章，各章内容安排如下：

第1章，绪论。介绍了课题的研究背景与意义，分析了国内外在热点残基预测和蛋白质设计领域的研究现状与不足之处，阐述了论文的主要研究内容和组织结构。

第2章，相关技术。简要介绍了蛋白质结构与相互作用的基础知识、ESM-2蛋白质语言模型、图神经网络与图注意力网络原理、RFdiffusion扩散模型、ProteinMPNN序列设计方法、RoseTTAFold3结构预测模型，以及Web应用开发所采用的前后端技术栈。

第3章，需求分析。对蛋白质Binder生成系统进行了全面的需求分析，包括系统的业务需求描述、功能性需求分析（通过用例图建模）、非功能性需求分析（包括性能、安全性、可用性等方面）。

第4章，系统设计。阐述了系统的总体架构设计、各核心模块的详细设计方案，包括热点预测模型架构设计、Binder设计pipeline流程设计、Web应用前后端架构设计、数据库设计和API接口设计。

第5章，系统实现。详细介绍了各模块的具体实现过程，包括数据预处理与特征提取、热点预测模型的训练与优化、RFD3骨架生成模块的调用与配置、ProteinMPNN序列设计模块的集成、RF3结构验证模块的实现，以及DeepBinder Web应用的前端界面和后端服务实现。

第6章，系统测试。对系统进行了全面的测试评估，包括热点预测模型的多维度性能评估（五折交叉验证、独立测试、Top-K分析、消融实验、基线对比）、Web应用的功能性测试和界面兼容性测试，并给出了测试结论。

第7章，总结与展望。对本文的研究工作进行了全面总结，分析了系统存在的不足之处，并对未来的改进方向和研究前景进行了展望。

---

## 第2章 相关技术

### 2.1 蛋白质结构与相互作用基础

#### 2.1.1 蛋白质结构层次

蛋白质是由氨基酸通过肽键连接而成的生物大分子，其结构可分为四个层次。一级结构是指氨基酸的线性序列，由20种标准氨基酸按照遗传信息的编码顺序排列而成。二级结构是指多肽链局部区域通过主链氢键形成的有规则的空间构象，主要包括α-螺旋（α-helix）和β-折叠（β-sheet）两种基本类型。三级结构是指整条多肽链在二级结构的基础上通过侧链间的疏水作用、氢键、离子键、范德华力和二硫键等作用力进一步折叠形成的三维空间构象。四级结构是指由两条或多条多肽链（亚基）通过非共价相互作用组装成的功能性蛋白质复合体。

在结构生物学研究中，蛋白质的三维结构通常以PDB（Protein Data Bank）格式进行存储和交换。PDB文件包含了蛋白质中各原子的三维坐标、残基编号、链标识、温度因子（B-factor）等详细信息，是计算结构生物学研究的基础数据格式。本研究中的热点残基预测和Binder设计均以PDB结构文件作为核心输入。

#### 2.1.2 蛋白质-蛋白质相互作用与热点残基

蛋白质-蛋白质相互作用（PPI）是指两个或多个蛋白质分子通过非共价键形成的特异性物理接触。PPI界面通常由10-30个氨基酸残基组成，界面的几何形状和化学性质决定了相互作用的特异性和亲和力。

在PPI界面上，不同残基对结合自由能的贡献存在显著差异。热点残基（Hotspot Residues）是指对结合自由能贡献最大的少数关键残基，其典型特征是丙氨酸扫描突变后结合自由能变化ΔΔG ≥ 2.0 kcal/mol。实验研究表明，热点残基通常具有以下特征：（1）位于结合界面的中心区域，远离溶剂暴露区；（2）富含色氨酸（Trp）、精氨酸（Arg）、酪氨酸（Tyr）等具有大侧链或带电荷的氨基酸类型；（3）在进化过程中高度保守，序列保守性高于非热点界面残基；（4）在空间上倾向于形成紧密的相互作用簇。准确识别热点残基对于理解蛋白质相互作用的分子机制和指导蛋白质药物设计具有重要的指导意义。

#### 2.1.3 丙氨酸扫描与热点残基鉴定

丙氨酸扫描突变（Alanine Scanning Mutagenesis）是鉴定热点残基的经典实验方法。其基本原理是将目标残基突变为侧链仅含一个甲基的丙氨酸（Alanine），通过测量突变前后结合自由能的变化（ΔΔG）来判断该残基对结合的贡献程度。如果ΔΔG ≥ 2.0 kcal/mol，则认为该残基为热点残基。尽管丙氨酸扫描是最可靠的实验鉴定方法，但其通量低、成本高，无法大规模应用于所有PPI系统的热点残基鉴定，因此开发准确的计算预测方法具有重要的应用价值。

### 2.2 蛋白质语言模型ESM-2

#### 2.2.1 预训练语言模型的发展

自然语言处理（NLP）领域的预训练语言模型，如BERT、GPT等，通过在海量文本数据上进行自监督预训练，学习到了丰富的语言表示，在各类下游任务中取得了优异表现。受此启发，研究者将类似的思想应用于蛋白质序列建模。蛋白质序列可以被视为由20种氨基酸字母组成的"语言"，包含了蛋白质结构、功能和演化历史的丰富信息。通过在大规模蛋白质序列数据库上进行预训练，蛋白质语言模型能够学习到氨基酸之间的依赖关系、残基级别的结构和功能特征，以及蛋白质家族的演化信息。

Rives等人于2019年首次将Transformer架构应用于蛋白质序列建模，利用UniRef50数据库中约2700万条蛋白质序列训练了一个具有650M参数的蛋白质语言模型。该模型的内部表示被发现与蛋白质的二级结构、三级接触和功能位点等生物学特性高度相关，表明预训练蛋白质语言模型能够有效地从海量序列数据中学习到生物学上有意义的特征表示。

#### 2.2.2 ESM-2模型架构

ESM-2（Evolutionary Scale Modeling 2）是Meta AI于2022年发布的蛋白质语言模型系列，包含从8M到15B参数的多个规模版本。本研究采用的esm2_t33_650M_UR50D版本具有33层Transformer、650M参数，在UniRef50数据集上进行了预训练，能够为每个输入氨基酸残基生成1280维的嵌入向量表示。

ESM-2的核心架构基于Transformer的Encoder部分，采用自注意力机制（Self-Attention）来捕获序列中任意位置间的长程依赖关系。与NLP的Transformer不同，ESM-2采用旋转位置编码（Rotary Position Embedding，RoPE）来编码残基在序列中的位置信息。RoPE通过旋转变换将相对位置信息融入到自注意力计算中，相比绝对位置编码具有更好的序列长度外推能力。

ESM-2的预训练任务采用掩码语言建模（Masked Language Modeling，MLM），即随机掩盖输入序列中的部分氨基酸，要求模型根据上下文预测被掩盖位置的氨基酸类型。通过这一自监督学习目标，模型被迫学习氨基酸序列的内在规律、局部结构偏好和长程相互作用模式。研究表明，ESM-2的嵌入表示能够有效捕获残基级别的二级结构倾向、溶剂可及性和功能位点等特征，其性能在多个蛋白质功能预测基准测试中显著优于传统的位置特异性打分矩阵（PSSM）和隐马尔可夫模型（HMM）谱特征。

在本研究中，ESM-2嵌入特征作为热点预测模型的核心输入，通过冻结预训练权重的特征提取方式，直接获取每个残基的1280维表示向量。相比传统的人工特征工程方法，这种方式无需耗时计算PSSM和HMM谱，且能够捕获更丰富的蛋白质演化信息。

### 2.3 图神经网络与图注意力网络

#### 2.3.1 图神经网络概述

图神经网络（Graph Neural Network，GNN）是专门用于处理图结构数据的深度学习模型。在蛋白质科学中，蛋白质结构可以很自然地表示为图：将每个氨基酸残基视为图中的节点（Node），将残基之间的空间邻近关系或序列相邻关系视为边（Edge）。这种图表示方法能够有效捕获蛋白质结构中非欧几里得的空间关系，克服了传统卷积神经网络（CNN）仅适用于规则网格数据的局限性。

GNN通过消息传递（Message Passing）机制进行信息传播：在每一层中，每个节点根据其邻居节点的特征来更新自身的表示。经过多层消息传递后，每个节点的表示将包含其局部邻域乃至更远距离的结构信息。对于热点残基预测任务，GNN能够学习到界面残基间的空间相互作用模式，这在传统的序列方法中是难以实现的。

GNN的主要变体包括图卷积网络（Graph Convolutional Network，GCN）、GraphSAGE、图注意力网络（GAT）等。GCN通过固定的归一化权重聚合邻居特征，GraphSAGE引入可学习的聚合函数提高了灵活性，而GAT则进一步通过注意力机制实现了邻居节点的自适应加权聚合。

#### 2.3.2 图注意力网络（GAT）

图注意力网络（Graph Attention Network，GAT）是Velickovic等人于2018年提出的GNN变体，其核心创新在于引入自注意力机制来动态计算节点间的注意力权重。与GCN使用固定的边权重不同，GAT通过可学习的注意力机制，使每个节点能够根据不同邻居的重要性自适应地分配聚合权重。对于蛋白质界面上的热点残基预测而言，这一特性尤为重要：界面上的关键残基可能对特定的邻居残基有更强的交互信号，GAT能够自动识别并增强对这些重要交互的关注。

GAT层的计算过程分为三个步骤。首先，计算节点间注意力系数：对每个节点i及其邻居j，将两者特征经线性变换后拼接，通过单层前馈网络和LeakyReLU激活函数计算原始注意力分数e_ij。其次，使用softmax函数对每个节点的所有邻居注意力分数进行归一化，得到注意力权重α_ij。最后，按注意力权重对邻居特征进行加权求和，得到节点i的更新表示。为进一步增强稳定性，GAT通常采用多头注意力（Multi-Head Attention）机制，即并行计算多组独立的注意力权重，将各组结果拼接或取平均作为最终输出。

在本研究中，GAT模型在热点预测中具有以下优势：（1）能够自适应地关注对结合重要的邻居残基；（2）对图的拓扑结构变化具有较强的鲁棒性；（3）注意力权重具有一定的可解释性，可以辅助分析哪些残基相互作用对预测结果贡献最大。

#### 2.3.3 Squeeze-and-Excitation模块

Squeeze-and-Excitation（SE）模块是Hu等人于2018年提出的一种通道注意力机制，通过对特征通道间的相互依赖关系进行显式建模，实现对不同通道的自适应重新校准。在本研究中，SE模块被应用于输入特征的通道维度，自动学习每种特征维度对热点预测任务的重要性权重。例如，在ESM-2嵌入的1280个维度中，某些维度可能主要编码残基的疏水性或电荷特征，这些维度对热点预测可能比编码其他属性的维度更为关键，SE模块能够自动识别并增强这些重要维度的贡献。

### 2.4 蛋白质设计相关深度学习模型

#### 2.4.1 RFdiffusion扩散模型

RFdiffusion是Watson等人于2023年提出的蛋白质骨架扩散模型，将计算机视觉领域取得巨大成功的去噪扩散概率模型（Denoising Diffusion Probabilistic Model，DDPM）应用于蛋白质骨架生成任务。其核心思想是通过渐进式的去噪过程来生成蛋白质骨架的三维坐标。

RFdiffusion的训练基于RoseTTAFold结构预测模型的架构，利用其对蛋白质结构和序列之间关系的深度理解。训练过程包括两个阶段：（1）前向扩散过程：向已知的天然蛋白质结构逐步添加高斯噪声，使骨架坐标逐渐模糊化直至完全随机；（2）反向去噪过程：训练一个条件神经网络从噪声中恢复原始结构，学习蛋白质骨架的内在分布规律。在生成阶段，从随机噪声出发，通过训练好的去噪网络逐步还原出符合目标条件的蛋白质骨架结构。

在本研究的Binder设计pipeline中，RFD3（RFdiffusion第三代版本）被用于条件Binder骨架生成：以目标蛋白的结构和预测的热点残基作为条件输入，引导扩散模型生成能够与目标蛋白指定界面特异性结合的Binder蛋白质骨架。

#### 2.4.2 ProteinMPNN序列设计方法

ProteinMPNN是Dauparas等人于2022年提出的基于图神经网络的蛋白质序列设计方法。其将蛋白质骨架结构编码为图，节点代表每个残基位置，节点特征包含主链二面角（φ、ψ、ω）和Cα-Cα距离等几何信息，边代表残基对的空间相对位置。通过在这种结构图上进行消息传递，ProteinMPNN学习预测每个位置最适合的氨基酸类型。

ProteinMPNN的关键优势包括：（1）与传统的Rosetta固定骨架序列设计相比，计算效率提升了数个数量级；（2）在天然蛋白质骨架上的序列恢复率（即预测序列与天然序列的一致率）达到52%，显著高于Rosetta的32%；（3）设计的序列在大肠杆菌表达系统中的可溶性表达成功率明显优于传统方法。最新版本的LigandMPNN进一步扩展了模型能力，支持在存在小分子配体或辅因子的条件下进行序列设计。

在本研究的Binder设计pipeline中，ProteinMPNN被用于为RFD3生成的Binder骨架设计氨基酸序列，每个骨架通常生成多个候选序列以供后续评估筛选。

#### 2.4.3 RoseTTAFold3结构预测模型

RoseTTAFold3（RF3）是华盛顿大学David Baker实验室于2024年发布的第三代蛋白质结构预测模型。RF3在原版RoseTTAFold和RF2的基础上进行了全面升级，其最突出的特点是实现了对多种生物分子复合物的统一建模，包括蛋白质-蛋白质复合物、蛋白质-核酸复合物和蛋白质-小分子复合物。

RF3的架构沿用了"双轨"设计思路：一轨在序列层面通过多序列比对（MSA）提取共演化信息，另一轨在结构层面通过三维坐标进行空间推理，两者通过交叉注意力层进行信息交换。RF3的另一个重要创新是将结构预测与扩散模型进行整合，在结构优化阶段采用类似扩散的迭代方法逐步改善预测精度。

在本研究的Binder设计pipeline中，RF3承担结构验证的角色：对ProteinMPNN设计的Binder序列进行结构预测，验证其是否能够折叠到预期的骨架结构，并评估Binder与目标蛋白形成的复合物构象的合理性。RF3输出的置信度指标（如pLDDT、PAE等）为Binder设计的质量评估提供了关键依据。

### 2.5 Web应用开发技术栈

#### 2.5.1 前端技术

本系统的Web前端采用React 18框架构建用户界面。React是由Meta公司开发的开源JavaScript库，采用组件化开发模式和虚拟DOM技术，能够高效地构建复杂的单页应用（SPA）。前端开发使用TypeScript语言以增强代码的类型安全性和可维护性，使用Vite作为构建工具以获得快速的开发服务器启动和热模块替换（HMR）体验。状态管理采用轻量级的Zustand库，通过简洁的API实现组件间状态共享。

蛋白质三维结构的可视化是系统前端的关键功能之一。本系统采用Molstar（Molecular Structure Viewer）作为3D可视化引擎。Molstar是PDBe（Protein Data Bank in Europe）开发的现代化分子可视化库，基于WebGL实现高性能的GPU加速渲染。Molstar支持多种分子结构文件格式（PDB、CIF等）的加载和显示，提供了丰富的可视化模式（卡通模式、球棍模式、表面模式等），支持选择集管理、着色方案自定义和交互式测量等高级功能，能够满足蛋白质结构分析和Binder设计结果展示的需求。

#### 2.5.2 后端技术

系统后端采用FastAPI框架，FastAPI是基于Python 3.7+的现代Web框架，利用Python类型注解实现了自动请求验证、序列化和交互式API文档生成。FastAPI底层基于Starlette异步框架和Pydantic数据验证库，具有极高的性能（与NodeJS和Go相当）和出色的开发体验。系统选用FastAPI的主要考虑因素包括：（1）与深度学习模型（PyTorch、DGL）统一使用Python技术栈，便于模型集成；（2）原生的异步支持适应长时间运行的Binder设计任务；（3）自动生成的OpenAPI（Swagger）文档便于前后端联调。

数据库方面，系统采用SQLite作为轻量级嵌入式数据库，存储用户信息、实验记录和任务状态等数据。SQLite无需独立部署数据库服务器，适合单机部署场景。通过SQLAlchemy ORM框架进行数据库操作，实现与具体数据库引擎的解耦。用户认证采用JWT（JSON Web Token）机制，用户登录后获得签名的Token，后续请求需携带Token以验证身份和权限。

### 2.6 本章小结

本章系统阐述了蛋白质Binder生成系统所涉及的核心技术。首先介绍了蛋白质结构和相互作用的基础知识，包括蛋白质结构层次、PPI热点残基的概念和实验鉴定方法。然后详细介绍了蛋白质语言模型ESM-2的原理和架构，以及其在残基表示学习中的优势。接着介绍了图神经网络和图注意力网络（GAT）的基本原理，以及SE通道注意力机制的作用。在蛋白质设计方面，介绍了RFdiffusion扩散模型、ProteinMPNN序列设计方法和RoseTTAFold3结构预测模型的工作原理。最后介绍了系统Web应用开发所采用的前后端技术栈。这些技术为后续章节的系统设计、实现和测试奠定了理论基础。

---

## 第3章 需求分析

### 3.1 系统总体需求概述

蛋白质Binder生成系统的设计目标是为生物信息学和蛋白质工程领域的研究人员提供一套完整、易用的Binder自动设计工具。通过深入调研蛋白质设计领域的实际工作流程和用户需求，本系统需要解决的核心问题包括：（1）如何准确识别目标蛋白结合界面上对结合起关键作用的热点残基，为Binder设计提供靶向指导；（2）如何根据目标蛋白结构和预测热点，自动生成能够与之特异性结合的Binder蛋白质；（3）如何对设计的Binder进行可靠的结构验证和质量评估；（4）如何为非专业用户提供直观、便捷的操作界面，降低计算蛋白质设计工具的使用门槛。

系统需要支持的典型业务场景如下：用户（如结构生物学家或药物化学家）拥有一段目标蛋白质的三维结构（PDB或CIF格式文件），希望设计能够与该目标蛋白特定表面区域特异性结合的Binder蛋白。用户通过Web界面上传目标蛋白结构文件，系统自动解析结构、识别结合界面、预测热点残基、生成Binder骨架、设计氨基酸序列，并输出经过结构验证的Binder候选分子列表，同时提供三维结构可视化以辅助用户评估设计结果。

### 3.2 功能性需求分析

功能性需求描述系统需要提供的功能和业务能力。本节采用面向对象方法，使用用例图对系统的功能需求进行建模分析。

#### 3.2.1 系统参与者

系统的主要参与者（Actor）包括以下三类：

（1）**普通用户（研究员）**：系统的核心使用者，通常为生物学或药物研发领域的研究人员，需要利用系统进行Binder设计。其关注点在于便捷地获得可靠的设计结果，无需深入了解底层模型的实现细节。

（2）**系统管理员**：负责系统配置、用户管理、计算资源调度和模型版本维护。需要监控系统运行状态，管理任务队列，确保系统的稳定运行。

（3）**外部计算引擎**：包括ESM-2特征提取服务、RFD3骨架生成引擎、ProteinMPNN序列设计引擎和RF3结构预测引擎。这些是系统依赖的外部计算服务，通过适配器模式与系统核心逻辑进行交互。

#### 3.2.2 系统用例分析

本系统的核心用例包括以下方面：

**（1）用户管理用例**

- 用户注册：研究人员通过邮箱和密码创建系统账户。
- 用户登录：已注册用户通过凭证登录系统，获取JWT访问令牌。
- 个人信息管理：用户查看和修改个人信息。

**（2）项目管理用例**

- 创建实验项目：用户创建新的Binder设计项目，输入项目名称和描述。
- 查看项目列表：用户浏览自己创建的所有项目及其状态。
- 管理项目：用户可以删除或归档不再需要的项目。

**（3）结构上传与解析用例**

- 上传目标蛋白结构：用户上传PDB或CIF格式的蛋白质结构文件。
- 结构解析与验证：系统自动解析上传文件的格式和内容，验证结构完整性。
- 三维结构可视化：通过Molstar引擎在浏览器中渲染蛋白质三维结构，支持缩放、旋转、选择等交互操作。

**（4）热点残基预测用例**

- 启动热点预测：用户选择目标蛋白的指定链后，触发热点残基预测分析。
- 查看预测结果：系统以可视化方式标注预测的热点残基，显示每个残基的预测得分。
- 调整预测阈值：用户可以调整热点判定阈值，筛选不同置信度的热点残基。

**（5）Binder设计用例**

- 配置设计参数：用户设置Binder设计的参数，如Binder长度范围、生成数量、热点约束条件等。
- 启动Binder设计：系统依次执行RFD3骨架生成、ProteinMPNN序列设计和RF3结构验证。
- 查看设计进度：用户实时查看pipeline各阶段的执行状态和进度。
- 浏览设计结果：系统以列表和可视化方式展示所有Binder候选，包括序列信息、结构预览、置信度评分等。
- 导出设计结果：用户下载生成的Binder结构文件（PDB格式）和序列文件（FASTA格式）。

**（6）任务管理用例**

- 查看任务队列：用户查看自己提交的所有任务及其状态（排队中、执行中、已完成、失败）。
- 获取任务结果：任务完成后，用户可以查看和下载相应的输出文件。

#### 3.2.3 系统功能模块划分

根据用例分析，系统可划分为以下功能模块：

（1）**用户认证模块**：负责用户注册、登录、Token管理等身份认证相关功能。

（2）**结构处理模块**：负责蛋白质结构文件的解析、验证和预处理，包括PDB/CIF格式解析、链分离、残基信息提取等功能。

（3）**热点预测模块**：负责执行热点残基预测，包括ESM-2特征提取、蛋白质图构建、GAT模型推理、预测结果输出等功能。

（4）**Binder设计模块**：负责执行端到端的Binder设计pipeline，包括RFD3调用适配、ProteinMPNN调用适配、RF3调用适配和pipeline编排等功能。

（5）**可视化模块**：负责蛋白质三维结构的Web端渲染和交互，包括结构加载、可视化模式切换、热点残基高亮标注、Binder-靶标复合物展示等功能。

（6）**实验管理模块**：负责实验项目的创建、查询、更新和删除，以及设计结果的管理和导出。

（7）**任务调度模块**：负责异步任务的队列管理、状态跟踪和资源调度，确保长时间运行的计算任务不会阻塞Web服务响应。

### 3.3 非功能性需求分析

#### 3.3.1 性能需求

（1）**响应时间**：Web页面的首屏加载时间应在3秒以内；结构上传和解析操作应在10秒内完成；热点预测请求应在60秒内返回结果（含ESM-2特征提取时间）。

（2）**并发处理能力**：系统应支持至少5个用户同时在线操作，任务调度模块应能管理至少10个并发计算任务。

（3）**资源占用**：单次热点预测推理的GPU显存占用应控制在4GB以内，确保在消费级GPU上也能正常运行。

#### 3.3.2 安全性需求

（1）**用户认证**：采用JWT令牌机制，令牌设置合理的过期时间，防止未授权访问。

（2）**数据安全**：用户上传的蛋白质结构文件和设计结果应隔离存储，不同用户之间不能相互访问对方的数据。敏感配置信息（如密钥、数据库连接串）通过环境变量注入，不硬编码在代码中。

（3）**输入验证**：对所有用户上传的文件进行格式和大小校验，防止恶意文件上传攻击。

#### 3.3.3 可用性需求

（1）**界面友好性**：Web界面应简洁直观，主要操作流程（上传→预测→设计→查看）应在3步内完成。关键操作提供明确的引导提示和帮助信息。

（2）**错误处理**：系统应提供友好的错误提示信息，当计算任务失败时，向用户说明失败原因和可能的解决建议，而非直接显示技术错误堆栈。

（3）**兼容性**：Web前端应兼容主流浏览器（Chrome、Firefox、Edge）的最新版本。

#### 3.3.4 可扩展性需求

（1）**模型可替换性**：热点预测模型和Binder设计各阶段模型应通过适配器模式进行抽象，便于后续升级模型版本或替换为其他模型。

（2）**功能可扩展性**：系统架构应支持后续添加新的分析功能（如分子动力学模拟、结合自由能计算等）而不需要重构核心代码。

### 3.4 本章小结

本章对蛋白质Binder生成系统进行了全面的需求分析。首先描述了系统的总体需求目标和典型业务场景，明确系统需要解决的核心问题。然后通过参与者和用例分析，识别了用户管理、项目管理、结构上传与解析、热点残基预测、Binder设计、任务管理等六大核心用例，并据此划分了七个功能模块。最后从性能、安全性、可用性和可扩展性四个维度分析了系统的非功能性需求，为后续的系统设计提供了明确的约束条件和质量目标。

---

## 第4章 系统设计

### 4.1 系统总体架构设计

#### 4.1.1 架构设计思路

本系统采用分层架构（Layered Architecture）设计模式，整体划分为表示层（Presentation Layer）、业务逻辑层（Business Logic Layer）、计算引擎层（Compute Engine Layer）和数据持久层（Data Persistence Layer）四个层次。采用这种分层设计的主要理由是：（1）将深度学习模型的复杂计算逻辑与Web应用的用户交互逻辑解耦，使各层能够独立演化和测试；（2）通过定义清晰的层间接口，支持计算引擎的灵活替换和扩展；（3）前端与后端之间通过RESTful API进行通信，实现前后端分离，便于独立开发和部署。

系统采用B/S（浏览器/服务器）架构，用户通过浏览器访问Web应用，无需安装任何客户端软件。后端服务统一部署在服务器上，负责请求路由、业务编排和与计算引擎的交互。计算密集型任务（如ESM-2特征提取、GAT模型推理、RFD3骨架生成等）通过异步任务队列在后台执行，避免阻塞Web请求响应。

#### 4.1.2 系统四层架构

**（1）表示层（Presentation Layer）**：即前端Web界面，采用React 18框架构建单页应用（SPA）。该层负责用户交互界面的渲染、蛋白质三维结构的可视化展示（使用Molstar引擎）、用户操作的捕获和响应。表示层通过HTTP/HTTPS协议调用业务逻辑层提供的RESTful API获取数据和服务。

**（2）业务逻辑层（Business Logic Layer）**：即后端服务，采用FastAPI框架实现。该层负责处理HTTP请求、执行业务逻辑编排、管理用户会话和权限认证、调度计算任务。业务逻辑层是系统的核心，协调表示层和计算引擎层之间的数据流和控制流。

**（3）计算引擎层（Compute Engine Layer）**：包含系统的核心算法模块。热点预测引擎集成了ESM-2特征提取器和GAT模型推理器；Binder设计引擎封装了RFD3、ProteinMPNN和RF3三个模型的调用接口。计算引擎层通过Adapter适配器模式与业务逻辑层交互，使上层不直接依赖具体的模型实现细节。

**（4）数据持久层（Data Persistence Layer）**：负责系统和业务数据的存储和检索。采用SQLite数据库存储用户信息、实验项目记录和任务状态；采用文件系统存储蛋白质结构文件、特征缓存、模型权重和设计输出结果。

#### 4.1.3 系统技术架构图

系统总体技术架构的层次关系如下：

```
┌─────────────────────────────────────────────────────┐
│                    表示层 (Frontend)                  │
│  React 18 + TypeScript + Zustand + Molstar          │
│  [结构上传] [3D可视化] [结果浏览] [项目管理]           │
└───────────────────────┬─────────────────────────────┘
                        │ RESTful API (HTTP/JSON)
┌───────────────────────▼─────────────────────────────┐
│                  业务逻辑层 (Backend)                 │
│  FastAPI + SQLAlchemy + JWT + Pydantic              │
│  [认证服务] [设计编排] [任务调度] [结果管理]           │
└───────────────────────┬─────────────────────────────┘
                        │ Adapter Interface
┌───────────────────────▼─────────────────────────────┐
│                 计算引擎层 (Compute)                  │
│  ┌──────────────┐ ┌──────────────┐                 │
│  │ 热点预测引擎  │ │ Binder设计引擎│                 │
│  │ ESM-2 + GAT  │ │RFD3+MPNN+RF3│                 │
│  └──────────────┘ └──────────────┘                 │
└───────────────────────┬─────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────┐
│                 数据持久层 (Data)                     │
│  SQLite [用户/实验/任务] + FileSystem [结构/特征/模型] │
└─────────────────────────────────────────────────────┘
```

### 4.2 热点预测模块设计

#### 4.2.1 模型整体架构设计

热点残基预测模块的核心是一个端到端的深度学习pipeline，其设计遵循"特征提取→图构建→GAT推理→分类输出"的四阶段流程。

**输入**：目标蛋白质PDB结构文件和残基列表。

**输出**：每个残基的热点预测概率（0-1之间的连续值），概率越高表示该残基越有可能是热点残基。

模型架构设计包含以下关键组件：

（1）**特征提取器**：利用预训练的ESM-2模型（esm2_t33_650M_UR50D）对蛋白质序列进行编码，为每个残基提取1280维嵌入向量。ESM-2的参数在训练过程中被冻结（freeze），仅作为固定特征提取器使用，避免对大规模预训练模型进行全参数微调导致的过拟合风险。此外，可选地拼接PSSM特征（20维）、HMM特征（30维）、DSSP特征（14维）和传统理化特征（25维）作为辅助信息。

（2）**蛋白质图构建器**：以氨基酸残基为节点，以Cα原子间距离为边构建规则。设定距离阈值为10埃（Å），若两残基Cα原子间距离小于该阈值，则在对应节点间创建一条边。边的特征由归一化距离和余弦相似度组成。每个节点的初始特征为对应残基的多源融合特征向量（默认维度：1280维ESM-2嵌入）。

（3）**GAT编码器**：由3层图注意力网络堆叠而成，每层包含4个注意力头。每层GAT后接Layer Normalization和ReLU激活函数，并采用残差连接（Residual Connection）以缓解深层网络的梯度消失问题。在输入层之前，应用Squeeze-and-Excitation（SE）通道注意力模块对输入的1280维特征进行自适应通道重校准，抑制噪声维度的干扰。输入特征首先经过两层MLP投影到隐藏维（64维），然后进入GAT层序列进行消息传递。

（4）**层次化池化与分类器**：收集所有GAT层的输出（包括输入投影），通过注意力池化（Attention Pooling）机制对多层表示进行自适应聚合。池化后的特征与最后一层GAT输出和SE重校准后的原始特征进行拼接，形成融合多尺度信息的最终表示。分类器由四层MLP构成，将融合特征逐步降维至2维输出（热点/非热点的logits）。

#### 4.2.2 损失函数设计

针对热点残基数据中正负样本严重不平衡的问题（正样本比例约1.76%），模型默认采用Focal Loss作为损失函数。Focal Loss通过在标准交叉熵损失基础上引入调制因子(1-pt)^γ，降低已分类正确样本的损失权重，使模型训练更加关注难分类样本和少数类样本。本模型中Focal Loss的超参数设置为α=0.7（正样本权重）、γ=2.0（聚焦参数）。

此外，模型支持Label Smoothing（标签平滑）策略，将硬标签（0或1）平滑为软标签（如[0.025, 0.975]），防止模型对训练样本过度自信，提高泛化能力。标签平滑因子设置为ε=0.05。同时提供了FocalLossWithLabelSmoothing组合损失函数和WeightedFocalLoss加权损失函数作为可选方案。

#### 4.2.3 训练策略设计

（1）**优化器**：基础GAT模型使用Adam优化器，初始学习率1e-3，权重衰减为0。GAT_v2模型使用AdamW优化器。

（2）**学习率调度**：基础GAT模型使用ReduceLROnPlateau调度器，以验证集PR-AUC为监控指标，衰减因子0.6，耐心值10个epoch。GAT_v2模型使用CosineAnnealingWarmRestarts调度器，T_0=10，T_mult=2。

（3）**训练正则化**：Dropout概率设置为0.5，梯度裁剪最大范数设置为1.0。使用早停（Early Stopping）策略，在验证集PR-AUC连续15个epoch无改善时终止训练。

（4）**批次处理**：由于不同蛋白质的残基数量差异较大，采用批次大小为1的逐蛋白训练方式，每个蛋白质单独构建为一个图进行前向传播和反向传播。对于残基数量较多的蛋白质，使用分层采样策略在每个蛋白质内平衡正负样本比例（默认负样本与正样本比例为1:1）。

（5）**五折交叉验证**：按PDB链为分组单位进行5折划分（KFold，random_state=42），确保同一PDB链的所有残基分配到同一折中，避免数据泄露。每折独立训练一个模型，最终以5折模型的平均性能作为模型能力的评估结果。

（6）**掩码训练**：对于缺少标签的残基（如位于界面之外或实验数据未覆盖的残基），将其标签标记为-1并在损失计算时排除，确保模型仅从已标注的残基中学习。

### 4.3 Binder设计Pipeline模块设计

#### 4.3.1 Pipeline流程设计

Binder设计pipeline的设计遵循"预测→生成→设计→验证"的线性流程，各阶段之间通过文件接口进行数据传递。Pipeline以目标蛋白结构文件和热点预测结果作为输入，以经过验证的Binder候选列表作为输出。各阶段的详细设计如下：

**阶段一：目标处理与热点预测**。接收用户上传的目标蛋白PDB/CIF结构文件，提取目标链的氨基酸序列和结构信息。调用热点预测模块对目标蛋白的结合界面残基进行打分，识别高置信度的热点残基。将热点残基的空间位置作为条件约束传递给下一阶段的骨架生成模块。

**阶段二：RFD3骨架生成**。以目标蛋白结构和热点残基约束作为条件输入，调用RFD3扩散模型生成Binder蛋白的骨架结构。RFD3从随机噪声开始，经过约50步的反向扩散去噪过程，逐步生成与目标蛋白结合界面几何兼容的Binder骨架。生成过程受热点残基位置的引导，偏向于在热点残基附近形成紧密的界面接触。该阶段可配置的参数包括：Binder长度范围（如60-120残基）、生成候选数量（如10-100个）、扩散步数等。

**阶段三：ProteinMPNN序列设计**。对RFD3生成的每个Binder骨架，调用ProteinMPNN模型进行固定骨架序列设计。ProteinMPNN以骨架的Cα坐标和主链几何参数为输入，为每个残基位置预测最合适的氨基酸类型。每个骨架通常生成3-8条候选序列，以提高获得可表达设计的概率。

**阶段四：RF3结构验证**。对ProteinMPNN设计的每条Binder序列及其与目标蛋白形成的复合物，调用RF3模型进行结构预测和验证。RF3输出包括：预测的复合物三维结构、每个残基的pLDDT置信度评分、残基对之间的预测对齐误差（PAE）矩阵。通过分析RF3输出的置信度指标，筛选出预测折叠可靠、界面几何合理的设计进行优先推荐。

#### 4.3.2 Adapter适配器设计

为了解耦业务逻辑层与具体计算引擎的实现细节，系统采用Adapter适配器模式对各个计算引擎进行封装。每个引擎对应一个适配器类，实现统一的接口：

- `HotspotPredictor`：封装ESM-2特征提取和GAT模型推理，提供`predict(pdb_path, chain_id) -> List[HotspotResult]`接口。
- `RFD3Adapter`：封装RFD3模型的调用，提供`generate_backbones(target_pdb, hotspots, config) -> List[Backbone]`接口。
- `MPNNAdapter`：封装ProteinMPNN模型的调用，提供`design_sequences(backbone, config) -> List[Sequence]`接口。
- `RF3Adapter`：封装RF3模型的调用，提供`validate_structure(complex_pdb) -> ValidationResult`接口。

适配器内部负责处理模型环境的初始化、输入数据的格式转换、模型调用的具体命令行或API交互、以及输出结果的解析和标准化。当需要升级模型版本或替换为其他模型时，仅需修改适配器的内部实现，不影响上层业务逻辑代码。

### 4.4 Web应用模块设计

#### 4.4.1 前端模块设计

前端采用React组件化架构进行设计。主要页面和组件包括：

（1）**布局组件（Layout）**：全局导航栏（包含系统Logo、主导航菜单、用户信息入口）、侧边栏（项目列表和快捷操作入口）、主内容区域的页面框架。

（2）**用户认证页面（AuthPage）**：登录表单和注册表单，支持表单校验和错误提示。

（3）**项目列表页面（ProjectsPage）**：以卡片或表格形式展示用户的实验项目，支持新建项目、搜索和删除操作。

（4）**设计工作台页面（DesignWorkspace）**：系统核心功能的集成页面。包含以下子组件：
- **结构上传区**：拖拽上传或文件选择器，支持PDB/CIF格式预览。
- **3D分子查看器**：基于Molstar的交互式三维蛋白质结构可视化组件，支持链选择、热点残基高亮标注、Binder结构叠加显示。
- **热点预测面板**：展示预测结果（每个残基的得分），提供阈值滑块过滤显示。
- **设计配置面板**：提供Binder设计参数的表单配置界面。
- **结果列表组件**：以表格形式展示设计完成的Binder候选，包含序列、置信度、操作按钮。

（5）**任务监控组件（TaskMonitor）**：实时显示后台任务的执行状态和进度，支持任务取消操作。

状态管理采用Zustand库，主要状态切片包括：`authStore`（用户认证状态）、`projectStore`（项目列表和当前项目）、`designStore`（设计参数、预测结果、设计结果）、`taskStore`（任务队列和状态）。

#### 4.4.2 后端模块设计

后端采用FastAPI框架，以模块化方式组织代码。主要模块包括：

（1）**认证模块（auth）**：处理用户注册（`POST /auth/register`）、登录（`POST /auth/login`）、令牌刷新（`POST /auth/refresh`）请求。密码使用bcrypt哈希存储，令牌使用python-jose库生成JWT。

（2）**设计模块（design）**：核心业务模块，提供Binder设计全流程的API接口。主要端点包括：
- `POST /design/predict-hotspots`：触发热点残基预测。
- `POST /design/generate-binder`：启动完整的Binder设计pipeline。
- `GET /design/tasks/{task_id}`：查询任务执行状态。
- `GET /design/results/{job_id}`：获取设计结果详情。

（3）**项目模块（projects）**：管理用户的项目，支持CRUD操作。

（4）**上传模块（upload）**：处理蛋白质结构文件的上传，包含文件格式验证和大小限制。

（5）**任务管理模块（jobs）**：管理异步任务的创建、状态更新和结果持久化。使用后台任务队列处理长时间运行的计算任务。

（6）**监控模块（monitor）**：提供系统运行状态监控接口，包括计算引擎可用性检查和资源使用统计。

### 4.5 数据库设计

本系统采用SQLite数据库，设计了以下主要数据表：

**（1）用户表（users）**
- id：主键，自增整数
- email：用户邮箱，唯一索引
- hashed_password：bcrypt哈希后的密码
- created_at：创建时间
- updated_at：更新时间

**（2）项目表（projects）**
- id：主键，自增整数
- name：项目名称
- description：项目描述
- user_id：外键，关联用户表
- created_at：创建时间
- updated_at：更新时间

**（3）结构文件表（structure_files）**
- id：主键，自增整数
- project_id：外键，关联项目表
- filename：原始文件名
- file_path：服务器存储路径
- chain_id：目标链标识
- sequence：蛋白质序列
- residue_count：残基数量
- status：处理状态（pending/processing/ready/error）
- created_at：上传时间

**（4）热点预测结果表（hotspot_predictions）**
- id：主键，自增整数
- structure_file_id：外键，关联结构文件表
- results_json：预测结果JSON（残基编号、得分、是否热点）
- threshold：使用的判定阈值
- created_at：预测时间

**（5）设计任务表（design_jobs）**
- id：主键，自增整数
- project_id：外键，关联项目表
- structure_file_id：外键，关联结构文件表
- status：任务状态（queued/running/completed/failed）
- config_json：设计参数配置JSON
- error_message：失败时的错误信息
- started_at：开始时间
- completed_at：完成时间

**（6）设计结果表（design_results）**
- id：主键，自增整数
- job_id：外键，关联设计任务表
- binder_sequence：Binder氨基酸序列
- backbone_pdb_path：骨架结构文件路径
- designed_pdb_path：设计结构文件路径
- validation_json：RF3验证结果JSON
- confidence_score：综合置信度评分
- created_at：创建时间

### 4.6 接口设计

系统前后端之间通过RESTful API进行通信，采用JSON格式进行数据交换。以下是主要API接口的设计规范：

**认证相关接口**：
- `POST /api/v1/auth/register`：用户注册，请求体包含email和password，返回用户信息和JWT令牌。
- `POST /api/v1/auth/login`：用户登录，请求体包含email和password，返回JWT令牌和过期时间。

**项目相关接口**：
- `GET /api/v1/projects`：获取当前用户的项目列表。
- `POST /api/v1/projects`：创建新项目。
- `DELETE /api/v1/projects/{id}`：删除指定项目。

**结构上传相关接口**：
- `POST /api/v1/structures/upload`：上传蛋白质结构文件（multipart/form-data）。
- `GET /api/v1/structures/{id}`：获取结构文件的解析结果。

**热点预测相关接口**：
- `POST /api/v1/hotspots/predict`：启动热点残基预测任务，请求体包含structure_id和chain_id。
- `GET /api/v1/hotspots/results/{task_id}`：获取热点预测任务的结果。

**Binder设计相关接口**：
- `POST /api/v1/design/generate`：启动Binder设计任务，请求体包含structure_id、hotspot_results_id和设计参数配置。
- `GET /api/v1/design/tasks/{task_id}`：查询设计任务状态。
- `GET /api/v1/design/results/{job_id}`：获取设计结果列表。
- `GET /api/v1/design/results/{job_id}/{result_id}/download`：下载指定设计结果的PDB文件。

所有受保护的API接口要求在请求头中携带`Authorization: Bearer {token}`进行身份验证。API响应采用统一格式：
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

### 4.7 本章小结

本章详细阐述了蛋白质Binder生成系统的设计方案。在总体架构方面，系统采用四层分层架构（表示层、业务逻辑层、计算引擎层、数据持久层）和B/S模式，通过前后端分离实现各层的独立演化和部署。在热点预测模块设计中，给出了模型的整体架构（特征提取→图构建→GAT编码→分类输出）、损失函数设计（Focal Loss + Label Smoothing）和训练策略设计（五折交叉验证、掩码训练、早停等）。在Binder设计pipeline中，定义了"预测→生成→设计→验证"四阶段流程，并通过Adapter模式解耦业务逻辑与计算引擎。在Web应用设计中，给出了前端组件树和后端模块的完整设计方案。最后详细设计了数据库表结构和RESTful API接口规范，为系统实现提供了全面的技术蓝图。

---

## 第5章 系统实现

### 5.1 开发环境与工具

本系统的开发环境配置和核心依赖如下：

**硬件环境**：
- CPU：Intel Xeon 处理器
- GPU：NVIDIA GPU（支持CUDA 12.1），用于ESM-2特征提取和GAT模型训练
- 内存：32GB+
- 存储：50GB SSD

**软件环境**：
- 操作系统：Linux（Ubuntu 22.04 / WSL2）
- Python版本：3.11
- CUDA版本：12.1
- 深度学习框架：PyTorch 2.0+、DGL（Deep Graph Library）2.0+
- 前端框架：React 18、TypeScript 5.0、Vite 5.0
- 后端框架：FastAPI 0.104+

**核心Python依赖**（hotspot-prediction/requirements.txt）：
- torch >= 2.0.0（深度学习框架）
- dgl >= 2.0.0（图神经网络库）
- transformers >= 4.30.0（HuggingFace Transformers，ESM-2模型加载）
- biopython >= 1.81（PDB文件解析）
- scikit-learn >= 1.3.0（评估指标和交叉验证）
- imbalanced-learn >= 0.11.0（SMOTE过采样）
- numpy >= 1.24.0, pandas >= 2.0.0（数据处理）
- matplotlib >= 3.7.0, seaborn >= 0.12.0（可视化）
- pydantic >= 2.0（配置管理）

**项目源代码结构**：
```
hotspot-prediction/
├── config.py                    # 全局配置（超参数、路径）
├── dataset.py                   # 数据集处理、图构建、特征提取
├── model.py                     # 模型定义（GAT/GAT_v2/Ensemble/MLP）
├── train.py                     # 训练循环、交叉验证
├── main.py                      # CLI入口（train/test/inference模式）
├── src/hotspot_prediction/      # 重构后的模块化代码
│   ├── config.py                # HotspotConfig 数据类
│   ├── cli.py                   # 命令行接口（prepare-data/train/evaluate）
│   ├── data/                    # 数据I/O和标准化
│   │   ├── normalize.py         # 数据标准化
│   │   ├── dataset.py           # 数据集类
│   │   └── io.py                # 文件读写
│   ├── features/                # 特征处理
│   │   └── graph.py             # 图构建
│   ├── baselines/               # 基线方法
│   │   └── rule_based.py        # 基于规则的基线
│   ├── training/                # 训练工具
│   │   ├── sampling.py          # 平衡采样
│   │   └── selection.py         # 模型选择
│   └── evaluation/              # 评估工具
│       └── metrics.py           # 评估指标计算
├── scripts/                     # 可执行脚本
│   ├── prepare_data.py          # 数据准备
│   ├── train.py                 # 训练脚本
│   ├── evaluate_test.py         # 独立测试集评估
│   └── generate_reports.py      # 报告生成
└── tests/                       # 单元测试
```

### 5.2 数据处理与特征提取实现

#### 5.2.1 数据标准化处理

系统的数据预处理流程在`src/hotspot_prediction/data/normalize.py`中实现。原始数据来源于PPI-hotspotID论文的公开数据集，包含data1（训练源）和data2（独立测试）两个Excel文件。

数据标准化处理的核心逻辑如下：

（1）读取原始Excel文件，解析每个PDB条目对应的突变记录、ΔΔG值和热点标签。

（2）将UniProt残基编号映射为PDB结构文件中的实际残基编号。这一步骤是数据处理的关键挑战：数据集中的突变记录以UniProt编号系统记录，而PDB结构文件使用独立的残基编号系统。系统通过解析PDB文件中的REMARK和SEQRES记录，建立UniProt编号到PDB编号的映射关系。

（3）按照ΔΔG ≥ 2.0 kcal/mol的阈值标注热点残基（正样本），其余残基标注为非热点（负样本）。

（4）对data2中的重复记录进行去重处理，保留唯一的PDB链-残基组合。

处理后的数据集统计信息如表5.1所示。

表5.1 处理后数据集统计

| 数据集 | 用途 | PDB链数 | 热点残基数 | 非热点残基数 | 正样本比例 |
|--------|------|---------|-----------|-------------|-----------|
| data1_train_val | 训练/验证 | 49 | 134 | ~10,000 | ~1.3% |
| data2_test | 独立测试 | 74 | 191 | 10,665 | 1.76% |
| strict_non_overlap | 审计 | 7 | 18 | — | — |

#### 5.2.2 ESM-2特征提取

ESM-2特征提取通过HuggingFace Transformers库加载预训练的`facebook/esm2_t33_650M_UR50D`模型实现。具体流程如下：

（1）使用Biopython的PDBParser解析目标蛋白质的PDB文件，提取每个残基的氨基酸类型和Cα原子坐标。

（2）将残基序列（单字母氨基酸编码）输入ESM-2模型。模型经过33层Transformer编码后，最后一层的隐藏状态作为每个残基的1280维嵌入向量。

（3）对于PDB文件中存在而ESM-2输出中也可能存在的插入残基（如结构中存在但序列比对中的gap），通过残基序号进行对齐，确保嵌入向量与蛋白质图节点的正确对应。

（4）提取的ESM-2嵌入特征缓存到`data/features/`目录下，以PDB ID和链标识作为文件名，避免重复计算。

#### 5.2.3 蛋白质图构建

蛋白质图构建在`src/hotspot_prediction/features/graph.py`中实现，将每个蛋白质转化为DGL图对象。构建步骤如下：

（1）提取每个残基的Cα原子三维坐标，构建残基间距离矩阵。

（2）根据10Å距离阈值（`MAP_CUTOFF = 10.0`）确定边的存在性：若两残基Cα距离小于阈值，则在图中创建一条无向边。

（3）为每条边计算特征：将Cα-Cα距离除以15.0（`DIST_NORM`）作为归一化距离特征；同时计算两个残基ESM-2嵌入向量之间的余弦相似度作为语义相似性特征。

（4）每个节点的特征为ESM-2嵌入向量与可选辅助特征（PSSM、HMM等）的拼接。默认配置下仅使用ESM-2嵌入，输入维度为1280。

（5）为每个节点设置标签：1表示热点残基（正样本），0表示非热点残基（负样本），-1表示标签缺失（掩码，损失计算时忽略）。

### 5.3 热点预测模型实现

#### 5.3.1 模型类实现

热点预测模型的核心实现在`model.py`文件中。以下按照模型的前向传播顺序，介绍各关键组件的实现。

**SELayer（Squeeze-and-Excitation层）**：

SE模块首先通过全局平均池化将每个通道的特征压缩为一个标量，然后通过两层全连接网络（含ReLU激活和Sigmoid门控）学习通道间的依赖关系。第一个全连接层采用1/16的压缩比以减少参数量。学习到的权重通过与原始输入逐元素相乘的方式，实现对不同特征通道的自适应重校准。

**PPIHotspotGAT（主模型）**：

模型的`forward`方法实现完整的前向传播计算图：

```
1. 输入特征x（shape: [N, 1280]，N为残基数）
2. SE通道重校准：x_se = SE(x)
3. 输入投影：h = InputProj(x_se) → shape: [N, 64]
4. GAT消息传递（3层，每层含残差连接和LayerNorm）：
   for i in range(3):
       h_new = GATConv_i(g, h).flatten(1) → shape: [N, 64]
       h_new = LayerNorm_i(h_new)
       h_new = ReLU(h_new)
       h = h + h_new  (残差连接)
       all_h.append(h)
5. 层次化注意力池化：
   h_stack = stack(all_h) → shape: [4, N, 64]
   attn_weights = softmax(AttentionPool(h_stack)) → shape: [4, N, 1]
   h_pooled = sum(h_stack * attn_weights) → shape: [N, 64]
6. 特征拼接：h_final = cat([h_pooled, h, x_se], dim=1) → shape: [N, 64+64+1280]
7. 分类器：logits = Classifier(h_final) → shape: [N, 2]
```

**PPIHotspotGAT_v2（增强版）**：

在基础GAT模型上的主要改进包括：（1）在SE模块前增加BatchNorm1d对输入特征进行批次归一化，加速训练收敛；（2）使用AdamW优化器替代Adam，将权重衰减与学习率解耦，提供更好的正则化效果；（3）使用CosineAnnealingWarmRestarts学习率调度器替代ReduceLROnPlateau，通过余弦退火和周期重启策略更充分地探索参数空间；（4）简化分类器结构，从四层MLP减少为三层，降低过拟合风险。

**PPIHotspotEnsemble（集成模型）**：

集成模型包含三个GAT子模型，分别具有2层、3层和4层GAT层。三个子模型的输出通过可学习的权重参数（初始化为均等权重1/3）进行加权组合。通过Softmax函数对权重进行归一化，确保权重和为1。集成策略能够综合不同深度的GAT模型的预测优势，降低单一模型的方差。

**PPIHotspotMLP（消融模型）**：

MLP消融模型移除了所有图结构处理组件（GAT层、图构建），仅保留特征提取和分类器部分。输入特征直接通过三层MLP进行分类。该模型作为消融实验的对照组，用于评估图结构信息对预测性能的贡献。

#### 5.3.2 损失函数实现

系统实现了四种损失函数，均继承自`nn.Module`：

（1）**FocalLoss**：在`forward`方法中，首先计算标准交叉熵损失（`F.cross_entropy`，reduction='none'），然后计算pt = exp(-ce_loss)，最后应用Focal调制因子：`loss = α * (1-pt)^γ * ce_loss`。α和γ分别默认为0.7和2.0。

（2）**LabelSmoothingLoss**：将硬目标标签转换为平滑标签。对于二分类问题（classes=2），正样本的目标概率从[0, 1]变为[ε/1, 1-ε]，其中ε=0.05。损失计算使用`log_softmax`与平滑目标的交叉熵。

（3）**FocalLossWithLabelSmoothing**：组合了Focal Loss和Label Smoothing的优点。先计算标签平滑后的交叉熵，再应用Focal调制因子。该损失函数同时在软标签约束下训练，并聚焦于难分类样本。

（4）**WeightedFocalLoss**：在FocalLoss基础上增加了类别权重因子。通过`pos_weight`参数放大正样本（热点残基）的损失贡献，适用于极端不平衡的场景。

#### 5.3.3 训练流程实现

训练的核心逻辑在`train.py`中实现。以下介绍关键的训练流程。

**数据加载与划分**：

系统使用`HotspotDataset`类加载所有已处理的蛋白质样本。每个样本是一个包含DGL图对象、节点特征张量、节点标签张量和元数据（PDB ID、链ID）的字典。使用`sklearn.model_selection.KFold`（n_splits=5, shuffle=True, random_state=42）进行交叉验证划分。以PDB链为分组单位，确保同一链的所有残基归入同一折。

**训练循环**：

每个epoch内，对训练集中的每个蛋白质图逐个进行前向传播、损失计算和反向传播：

```
for graph, features, labels in train_loader:
    optimizer.zero_grad()
    logits = model(graph, features)
    # 掩码处理：仅计算标签非-1位置的损失
    mask = labels != -1
    if mask.sum() > 0:
        loss = criterion(logits[mask], labels[mask])
        loss.backward()
        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
        optimizer.step()
```

**验证与模型选择**：

每个epoch结束后，在验证集的所有蛋白质上计算综合评估指标。主要监控指标为PR-AUC（精确率-召回率曲线下面积），适用于评估模型在不平衡数据集上的排序能力。当验证PR-AUC连续15个epoch无改善时，触发早停。最佳模型权重根据验证PR-AUC的最高值进行保存。

**掩码训练机制**：

针对每个蛋白质图中仅有部分残基具有标注（界面残基有ΔΔG数据）的问题，系统实现了掩码训练机制。在损失计算时，通过`labels != -1`的逻辑掩码排除无标签残基，确保模型仅在已标注的残基上进行监督学习。这一设计允许模型同时利用整个蛋白质图的结构信息（通过GAT的消息传递覆盖所有节点），但仅在标注子集上接受监督信号。

### 5.4 Binder骨架生成实现

Binder骨架生成功能通过调用RFD3模型实现。在`binder-design-system/utils/rfd3_runner.py`中实现了RFD3的调用适配逻辑。

**输入准备**：

（1）将目标蛋白的PDB结构文件转换为RFD3所需的输入格式。

（2）根据热点预测结果，生成热点残基约束文件（JSON格式），指定需要靶向的残基编号和链标识。约束信息被编码为RFD3的条件输入，引导扩散模型在热点残基附近生成Binder界面。

（3）设置生成参数：Binder长度范围（默认60-120个残基）、每个长度的生成数量、扩散步数（默认50步）、温度参数等。

**扩散生成过程**：

RFD3的生成过程从高斯随机噪声开始，经过T步迭代去噪逐步生成蛋白质骨架坐标。在每一步中，去噪网络接收当前的噪声骨架、目标蛋白结构和热点约束信息，预测去噪后的骨架坐标。热点约束通过在目标残基附近施加空间偏向性的能量项来实现：在反向扩散过程中，生成的Binder残基位置受到目标热点残基距离范围的软约束，促使其向结合界面靠拢。

**输出处理**：

生成的骨架以PDB格式保存，包含Binder蛋白的主链原子（N、Cα、C、O）坐标。每个生成的骨架具有唯一的标识符，并与对应的目标蛋白结构关联存储。生成结果还包括RFD3输出的内在质量评分，用于初步筛选。

### 5.5 序列设计与结构验证实现

#### 5.5.1 ProteinMPNN序列设计

序列设计在`binder-design-system/utils/mpnn_runner.py`中实现。调用流程如下：

（1）将RFD3生成的Binder骨架PDB文件转换为ProteinMPNN的输入格式。

（2）配置ProteinMPNN的推理参数：采样温度（默认0.1，控制序列多样性）、每骨架设计序列数（默认8条）、是否使用同源寡聚体模式等。

（3）调用ProteinMPNN进行推理。ProteinMPNN通过消息传递网络在骨架约束下逐位置预测氨基酸分布，然后根据温度参数从分布中采样具体氨基酸类型。

（4）为每个设计序列生成FASTA格式文件和全原子PDB结构文件（使用侧链构象的快速优化算法）。

#### 5.5.2 RF3结构验证

结构验证在`binder-design-system/utils/rf3_runner.py`中实现。验证流程如下：

（1）构建Binder-目标蛋白复合物的输入文件。将ProteinMPNN设计的Binder序列与目标蛋白序列合并为复合物输入。

（2）调用RF3模型预测复合物的三维结构。RF3通过序列-结构双轨网络，利用多序列比对（MSA）和结构模板信息进行高精度结构预测。

（3）解析RF3输出的关键质量指标：pLDDT（预测的局部距离差异检验）评分，取值范围0-100，越高表示预测越可靠，通常>70表示可信预测；PAE（预测对齐误差）矩阵，描述预测结构中各残基对的相对位置误差，界面区域的低PAE值表示Binder-靶标相互作用的几何关系预测可靠。

（4）根据质量指标对设计结果进行过滤和排序。筛选标准：Binder整体平均pLDDT > 70，Binder-靶标界面区域残基的平均pLDDT > 60，界面区域的PAE < 10Å。

### 5.6 Web应用实现

#### 5.6.1 前端界面实现

DeepBinder前端基于React 18 + TypeScript构建，主要页面的实现如下：

**设计工作台页面（DesignWorkspace）**：此页面是系统的核心操作界面，整合了结构上传、3D可视化、热点预测结果展示和Binder设计参数配置等功能。页面布局采用左侧面板+中央查看器的双栏设计。左侧面板包含可折叠的功能区块（结构信息、热点预测、设计配置），中央区域为Molstar 3D查看器和设计结果列表。

**3D分子查看器实现**：通过集成Molstar库（`molstar/lib/mol-canvas3d/PDBeMolstarCanvas3D`）实现。查看器支持以下交互功能：（1）蛋白质结构的卡通模式、球棍模式和表面模式渲染切换；（2）基于预测得分的颜色映射标注（热点残基使用红色-白色梯度着色，得分越高红色越深）；（3）鼠标拖拽旋转、滚轮缩放和平移操作；（4）链选择器，支持目标链和非目标链的独立显示与隐藏。热点预测结果通过Molstar的选择集（Selection）和颜色主题（ColorTheme）机制实现可视化标注。

**状态管理实现**：使用Zustand管理全局应用状态。`designStore`是核心状态切片，包含以下关键状态和方法：
- `currentStructure`：当前加载的蛋白质结构信息。
- `hotspotResults`：热点预测结果数组，每项包含残基编号、氨基酸类型和预测概率。
- `designJobs`：设计任务列表及其状态。
- `designResults`：已完成的设计结果列表。
- `startHotspotPrediction(structureId, chainId)`：触发热点预测异步方法。
- `startBinderDesign(structureId, config)`：启动Binder设计异步方法。

#### 5.6.2 后端服务实现

**FastAPI应用入口**（`binder-design-system/backend/main.py`）：

应用通过`FastAPI()`实例化，配置了CORS中间件以允许前端跨域请求。在应用启动事件（`@app.on_event("startup")`）中初始化数据库连接和计算引擎适配器。

**设计编排服务**（`binder-design-system/backend/services/design_pipeline.py`）：

`DesignPipeline`类是Binder设计全流程的核心编排逻辑。其`execute`方法按序调用四个阶段：

```
async def execute(self, structure_id, config):
    # 阶段一：热点预测
    hotspots = await self.hotspot_predictor.predict(structure, chain)
    
    # 阶段二：RFD3骨架生成
    backbones = await self.rfd3.generate(structure, hotspots, config)
    
    # 阶段三：ProteinMPNN序列设计
    for backbone in backbones:
        sequences = await self.mpnni.design(backbone, config)
    
    # 阶段四：RF3结构验证
    for seq in sequences:
        validation = await self.rf3.validate(structure, seq)
    
    return design_results
```

**异步任务处理**：

由于Binder设计的全流程耗时较长（全程可能超过30分钟），系统将设计任务作为后台异步任务执行。使用FastAPI的`BackgroundTasks`将设计任务放入后台队列，立即向客户端返回任务ID。前端通过轮询`GET /api/v1/design/tasks/{task_id}`接口获取任务状态更新。任务在执行过程中通过数据库更新状态字段（queued → running → completed/failed），前端根据状态变化实时更新界面。

### 5.7 本章小结

本章详细阐述了蛋白质Binder生成系统的实现细节。在数据处理方面，介绍了数据标准化处理流程、ESM-2特征提取和蛋白质图构建的具体实现。在热点预测模型方面，以PPIHotspotGAT为核心，详细描述了SE模块、多层GAT编码器、层次化注意力池化和分类器的实现逻辑，以及四种损失函数和训练流程的实现。在Binder设计方面，分别介绍了RFD3骨架生成、ProteinMPNN序列设计和RF3结构验证的调用适配实现。在Web应用方面，介绍了基于React和Molstar的前端界面实现，以及基于FastAPI的后端服务和异步任务处理实现。系统实现严格遵循第4章的设计方案，各模块之间通过清晰定义的接口进行交互。

---

## 第6章 系统测试

### 6.1 测试方案与策略

本系统的测试分为两个层面：热点预测模型的算法性能测试和Web应用的功能性测试。热点预测模型测试采用定量评估方法，通过多项统计指标全面衡量模型的预测能力和泛化能力。Web应用测试采用黑盒测试方法，设计测试用例覆盖主要功能流程和边界情况。

热点预测模型的测试策略遵循以下原则：（1）通过五折交叉验证评估模型的稳定性和一致性；（2）在独立测试集（data2）上评估模型的泛化能力，测试集不参与任何训练或超参数调优过程；（3）使用固定阈值0.5作为主要判定阈值，避免在测试集上搜索最优阈值导致的结果偏差；（4）同时报告残基级别分类指标和蛋白质级别Top-K推荐指标，从多角度反映模型的实用价值。

### 6.2 热点预测模型测试

#### 6.2.1 五折交叉验证

在data1训练/验证集上进行了五折交叉验证实验。以PDB链为分组单位进行划分，每个折叠使用不同的训练/验证划分。模型为PPIHotspotGAT，使用Focal Loss损失函数，训练最多200个epoch并启用早停机制。五折交叉验证的详细结果如表6.1所示。

表6.1 五折交叉验证结果

| 折号 | 最佳Epoch | ROC-AUC | PR-AUC | F1 Score | MCC | 正样本数 | 负样本数 |
|------|----------|---------|--------|----------|-----|---------|---------|
| Fold 1 | 8 | 0.7293 | 0.0688 | 0.0476 | 0.0392 | 32 | 987 |
| Fold 2 | 20 | 0.8720 | 0.1384 | 0.1176 | 0.1299 | 26 | 1377 |
| Fold 3 | 4 | 0.7814 | 0.0618 | 0.1163 | 0.1092 | 24 | 1633 |
| Fold 4 | 2 | 0.6921 | 0.0555 | 0.0000 | 0.0000 | 28 | 1407 |
| Fold 5 | 13 | 0.8133 | 0.1004 | 0.1622 | 0.1445 | 24 | 997 |

五折平均ROC-AUC为0.7776±0.0658，平均PR-AUC为0.0850±0.0330。交叉验证结果反映出两个现象：（1）各折之间的性能波动较大，这是由数据集规模小（每折训练集约39个PDB链）和正负样本极度不平衡所导致的；（2）Fold 4的性能异常（F1=0），分析发现该折中模型倾向于将所有残基预测为非热点，这一现象在小样本不平衡分类中较为常见。

#### 6.2.2 独立测试集评估

使用在data1上训练的最佳模型（选择验证集PR-AUC最高的checkpoint），在独立测试集data2上进行了评估。评估使用固定阈值0.5进行二分类判定。结果如表6.2所示。

表6.2 独立测试集评估结果（阈值=0.5）

| 指标 | 数值 |
|------|------|
| ROC-AUC | 0.7564 |
| PR-AUC | 0.1579 |
| Accuracy | 0.9818 |
| Precision | 0.4598 |
| Recall (Sensitivity) | 0.2094 |
| Specificity | 0.9956 |
| F1 Score | 0.2878 |
| MCC | 0.3023 |

混淆矩阵详情如表6.3所示。

表6.3 独立测试集混淆矩阵

| | 预测正类（热点） | 预测负类（非热点） | 合计 |
|------|------|------|------|
| 实际正类（热点） | TP = 40 | FN = 151 | 191 |
| 实际负类（非热点） | FP = 47 | TN = 10618 | 10665 |
| 合计 | 87 | 10769 | 10856 |

从上述结果可以看出，模型在高度不平衡的测试集（正样本仅占1.76%）上表现出了非平凡的预测能力。准确率（0.9818）和特异度（0.9956）较高，表明模型对非热点残基的识别较为准确，假阳性率较低。精确率（0.4598）意味着模型预测为热点的残基中约46%确实是热点，这在极度不平衡的分类任务中是一个可接受的水平。召回率（0.2094）偏低，反映出模型在小样本条件下对热点残基的查全能力有限。

MCC（Matthews相关系数）值为0.3023，显著优于随机猜测（MCC≈0），表明模型的预测与真实标签之间存在中等程度的正相关性。PR-AUC（0.1579）虽然绝对值不高，但远高于随机分类器的PR-AUC基准（0.0176，即正样本比例），说明模型具备有效的排序能力。

#### 6.2.3 平衡评估分析

为了与同类方法（如DeepHotResi）的评估协议对齐，也进行了平衡评估。平衡评估从负样本中随机抽取与正样本等量的样本进行评估，消除了类别不平衡对指标的影响。结果如表6.4所示。

表6.4 平衡评估结果

| 指标 | 数值 |
|------|------|
| Balanced ROC-AUC | 0.7686 |
| Balanced PR-AUC | 0.8105 |
| Balanced Precision | 1.0000 |
| Balanced Recall | 0.2094 |
| Balanced F1 | 0.3463 |
| Balanced MCC | 0.3420 |

在平衡评估条件下，PR-AUC从0.1579大幅提升至0.8105，精确率达到1.0，体现了模型在不平衡场景下的排序能力。需要指出的是，平衡评估反映的是模型的相对排序能力而非真实使用场景中的绝对性能，仅作为与同类方法对比的参考指标。

#### 6.2.4 Top-K热点推荐分析

热点预测的核心应用场景之一是推荐少量候选残基供实验验证。Top-K指标衡量模型能否将真实热点残基排在预测列表的前列，这对于实际应用具有更直接的参考价值。在data2测试集上的Top-K分析结果如表6.5所示。

表6.5 Top-K热点推荐分析

| Top-K | 含热点的蛋白数 | 命中蛋白数 | 蛋白命中率 | 总热点数 | 命中热点数 | 热点召回率 | 总筛选残基数 |
|-------|-------------|-----------|-----------|---------|-----------|-----------|------------|
| K=3 | 60 | 23 | 38.33% | 191 | 39 | 20.42% | 180 |
| K=5 | 60 | 28 | 46.67% | 191 | 55 | 28.80% | 300 |
| K=10 | 60 | 31 | 51.67% | 191 | 70 | 36.65% | 600 |

Top-K分析的结果表明：（1）当推荐每蛋白前3个候选残基时，38.33%的蛋白质中至少有一个真实热点残基被命中；（2）当推荐前10个候选残基时，蛋白质命中率达到51.67%，热点残基召回率达到36.65%；（3）随着K值增加，命中率和召回率均显著提升，但需要筛选的候选残基数量也相应增加。

与规则基线的对比结果如表6.6所示。

表6.6 与规则基线的Top-K对比

| 方法 | Top-3蛋白命中率 | Top-5蛋白命中率 | Top-10蛋白命中率 | 热点召回率(Top-10) |
|------|---------------|---------------|-----------------|------------------|
| 规则基线 | 16.67% | 21.67% | 35.00% | 15.18% |
| ESM-2 + GAT（本文） | 38.33% | 46.67% | 51.67% | 36.65% |

本文方法在所有Top-K指标上均显著优于规则基线，Top-3蛋白命中率提升21.66个百分点，Top-10热点召回率提升21.47个百分点，充分验证了基于深度学习的预测方法在热点残基排序推荐中的有效性和实用性。

#### 6.2.5 消融实验

为分析不同特征和模型结构组件对预测性能的贡献，进行了消融实验。消融实验结果如表6.7所示。

表6.7 消融实验独立测试集结果

| 模型配置 | ROC-AUC | PR-AUC | F1 | MCC |
|----------|---------|--------|-----|-----|
| 规则基线 | 0.5597 | 0.0252 | 0.0522 | 0.0322 |
| ESM-2 + MLP | — | — | — | — |
| ESM-2 + GAT（本文主模型） | 0.7564 | 0.1579 | 0.2878 | 0.3023 |

消融实验结果揭示了几个重要发现：（1）规则基线（基于氨基酸类型和位置的简单评分）的性能远低于深度学习方法（ROC-AUC仅0.5597），说明热点残基的预测是一个复杂的模式识别问题，需要深度学习模型的表示能力；（2）ESM-2嵌入特征配合GAT图结构建模能够有效提升预测性能，图结构所编码的残基空间相互作用关系对热点识别具有重要价值。

### 6.3 Web应用功能测试

#### 6.3.1 测试用例设计

针对DeepBinder Web应用的核心功能，设计了一系列测试用例以验证系统的功能完整性和交互正确性。测试用例覆盖用户认证、结构上传、热点预测和Binder设计等主要业务流程。

表6.8 Web应用功能测试用例

| 序号 | 用例名称 | 测试步骤 | 预期结果 | 测试结果 |
|------|---------|---------|---------|---------|
| 1 | 正向测试—用户注册—有效信息 | 1. 输入有效邮箱和密码 2. 点击注册按钮 | 注册成功，跳转到登录页 | 与预期结果一致 |
| 2 | 逆向测试—用户注册—无效邮箱 | 1. 输入无效格式邮箱 2. 点击注册按钮 | 注册失败，提示邮箱格式错误 | 与预期结果一致 |
| 3 | 正向测试—用户登录—正确凭证 | 1. 输入已注册邮箱和正确密码 2. 点击登录按钮 | 登录成功，跳转到项目列表页 | 与预期结果一致 |
| 4 | 逆向测试—用户登录—错误密码 | 1. 输入正确邮箱但错误密码 2. 点击登录按钮 | 登录失败，提示密码错误 | 与预期结果一致 |
| 5 | 正向测试—结构上传—PDB文件 | 1. 点击上传区域 2. 选择有效的PDB文件 3. 等待解析完成 | 上传成功，3D查看器显示蛋白质结构 | 与预期结果一致 |
| 6 | 逆向测试—结构上传—非PDB文件 | 1. 点击上传区域 2. 选择非结构文件（如.txt） | 上传失败，提示文件格式不支持 | 与预期结果一致 |
| 7 | 正向测试—3D可视化—交互操作 | 1. 在3D查看器中拖拽旋转 2. 滚动缩放 3. 点击残基查看详情 | 各项交互操作正常响应 | 与预期结果一致 |
| 8 | 正向测试—热点预测—执行预测 | 1. 选择目标蛋白链 2. 点击开始预测按钮 3. 等待预测完成 | 预测完成，热点残基在3D查看器中高亮显示 | 与预期结果一致 |
| 9 | 正向测试—项目列表—查看项目 | 1. 进入项目列表页 2. 查看已有项目 | 正确显示用户的所有项目及状态 | 与预期结果一致 |
| 10 | 界面兼容性测试—不同浏览器 | 在Chrome和Firefox中分别执行用例5和用例7 | 两种浏览器中功能均正常 | 与预期结果一致 |

#### 6.3.2 测试结果

Web应用功能测试共执行10个测试用例，全部通过，通过率100%。主要测试发现如下：

（1）**用户认证模块**：注册和登录功能正常，输入验证和错误提示机制有效。JWT令牌正确生成和验证。

（2）**结构上传与解析模块**：支持PDB和CIF格式文件的上传，文件格式验证有效。PDB文件解析正确提取了链信息和残基坐标。

（3）**3D可视化模块**：Molstar查看器渲染流畅，卡通和表面两种显示模式切换正常。热点残基的颜色标注清晰可辨。用户交互操作（旋转、缩放、平移）响应灵敏。

（4）**热点预测模块**：预测任务能够正确提交和执行，预测结果正确返回并高亮标注在3D查看器中。

（5）**项目管理模块**：项目创建、查询和删除功能正常，数据隔离正确（用户A无法访问用户B的项目）。

（6）**跨浏览器兼容性**：在Chrome和Firefox两种主流浏览器中，系统功能均表现一致，未发现兼容性问题。

### 6.4 测试结论

通过系统性的测试评估，可以得出以下结论：

（1）热点预测模型在高度不平衡的小样本数据集上表现出了有效的预测能力。独立测试集ROC-AUC为0.7564，MCC为0.3023，显著优于随机分类器和规则基线方法。Top-10蛋白质命中率达到51.67%，表明模型在实际应用中能够有效缩小热点残基的候选范围。

（2）模型性能受到数据集规模和类别不平衡的显著影响。五折交叉验证中各折间性能波动较大，召回率偏低（0.2094），反映出在极小正样本条件下训练深度学习模型的固有困难。这一问题需要通过更大规模、更高质量的训练数据来解决。

（3）Binder设计pipeline的技术可行性得到验证。通过Adapter模式集成RFD3、ProteinMPNN和RF3三个模型，实现了从目标蛋白结构输入到Binder候选输出的自动化流程。

（4）Web应用的功能完整性和可用性通过测试验证，各功能模块运行正常，用户界面友好，能够满足蛋白质Binder设计的基本操作需求。

### 6.5 本章小结

本章对蛋白质Binder生成系统进行了全面的测试评估。在热点预测模型测试方面，通过五折交叉验证（平均ROC-AUC 0.7776）、独立测试集评估（ROC-AUC 0.7564、MCC 0.3023）、Top-K分析（Top-10蛋白命中率51.67%）、消融实验和基线对比，多维度验证了基于ESM-2和GAT的预测方法的有效性。在Web应用测试方面，通过10个功能测试用例覆盖了用户认证、结构上传、3D可视化、热点预测和项目管理等核心功能，全部测试用例通过，验证了系统的功能完整性和可用性。

---

## 第7章 总结与展望

### 7.1 工作总结

本文围绕基于深度学习的蛋白质Binder生成系统这一课题，完成了从需求分析、系统设计、模型实现到系统测试的完整研发流程。主要工作成果总结如下：

（1）**设计并实现了基于ESM-2和图注意力网络的热点残基预测方法**。该方法利用预训练蛋白质语言模型ESM-2（650M参数版本）提取1280维残基嵌入特征，将蛋白质结构建模为图，通过多层图注意力网络（GAT）和Squeeze-and-Excitation通道注意力机制进行特征学习，结合Focal Loss和标签平滑策略处理样本不平衡问题。在PPI-hotspotID数据集上，独立测试集ROC-AUC为0.7564，MCC为0.3023，Top-10蛋白命中率为51.67%，显著优于规则基线方法，验证了该方法的有效性。

（2）**构建了端到端的Binder自动设计pipeline**。整合了RFD3扩散模型（Binder骨架生成）、ProteinMPNN图神经网络（序列设计）和RoseTTAFold3结构预测模型（结构验证），实现了从目标蛋白结构输入到Binder候选序列输出的全流程自动化。通过Adapter适配器设计模式解耦了业务逻辑与计算引擎的实现细节，提高了系统的可维护性和可扩展性。

（3）**开发了DeepBinder Web应用系统**。基于前后端分离架构，前端采用React 18 + TypeScript + Molstar技术栈实现了蛋白质三维结构的交互式可视化和用户友好的操作界面；后端采用FastAPI框架构建了RESTful API服务，结合JWT认证、SQLite数据持久化和异步任务队列，为用户提供了完整的Binder设计项目管理功能。

（4）**进行了系统的测试与评估**。通过五折交叉验证、独立测试集评估、Top-K热点推荐分析、消融实验和基线方法对比，多维度验证了热点预测模型的性能和局限性。对Web应用进行了全面的功能测试，验证了系统的完整性和可用性。

### 7.2 研究局限性

在本研究过程中，也识别出了以下需要正视的局限性：

（1）**数据规模与质量问题**。训练数据集仅有49个PDB链的134个热点残基标注，对于深度学习模型的充分训练而言规模偏小。正负样本极度不平衡（正样本比例约1.3%），导致模型召回率偏低。此外，数据集的非冗余性有限，训练集和测试集之间存在的UniProt/PDB重叠增加了过拟合乐观估计的风险。

（2）**特征完整性问题**。由于部分传统特征（如基于AmberTools的自由能分解、基于FreeSASA的溶剂可及表面积精确计算）的提取流程尚未完全打通，模型的输入特征以ESM-2嵌入为主，未能充分利用所有已知对热点预测有用的物理化学和演化特征。这使得与PPI-hotspotID等使用完整特征集的传统机器学习方法的公平对比存在一定困难。

（3）**模型可解释性不足**。虽然GAT的注意力权重在理论上可以提供一些可解释性信号（如哪些氨基酸之间的相互作用被模型重点关注），但本文尚未对此进行深入分析和可视化呈现。在药物设计等对可靠性要求极高的应用场景中，模型的可解释性是影响用户采纳意愿的重要因素。

（4）**Binder设计pipeline的生物学验证缺失**。由于实验验证的周期和成本限制，本文仅完成了Binder设计pipeline的技术实现和集成调试，尚未通过湿实验（如蛋白表达纯化、表面等离子体共振SPR、生物膜干涉BLI等结合实验）对设计的Binder进行实验验证。

### 7.3 未来工作展望

针对上述局限性，未来的工作可以从以下几个方面展开：

（1）**数据增强与扩充**。收集和整合更多的热点残基标注数据，如SKEMPI 2.0、AB-Bind等数据库中的结合自由能突变数据。利用半监督学习和主动学习策略，在有限的标注数据条件下最大化模型性能。探索利用ESM-2自监督预训练特征进行数据增强的方法。

（2）**多特征融合与模型升级**。打通完整的传统特征提取流程（PSSM、HMM、DSSP、SASA等），研究ESM-2嵌入与传统物理化学特征的最佳融合策略。探索更大规模的ESM-2模型版本（如3B、15B参数版本）对预测性能的影响。尝试将蛋白质结构预测模型（如AlphaFold2）的结构嵌入作为额外的输入特征。

（3）**模型架构的进一步探索**。尝试更先进的GNN架构，如基于等变特性的SE(3)-Transformer、基于几何深度学习的GemNet等。探索将蛋白质语言模型与结构模型进行端到端联合微调的可能性。引入对比学习和度量学习策略以增强模型的表示能力。

（4）**完善Binder设计pipeline**。增强热点预测结果对RFD3生成的条件约束效果，实现热点级别的精准界面设计指导。增加Binder成药性评估模块，如免疫原性预测、聚集倾向预测、稳定性评估等。开发更智能的设计结果筛选和排序策略。

（5）**实验验证与迭代优化**。选择若干代表性的设计案例进行湿实验验证（蛋白表达纯化、结合亲和力测定、结构验证），获得真实的实验反馈。基于实验反馈数据进行模型迭代优化，形成"计算设计→实验验证→模型优化"的闭环研究范式。

（6）**系统功能扩展**。在Web应用中增加批量设计、设计历史对比、用户协作分享等高级功能。优化计算资源调度策略，支持多GPU并行和云端弹性计算。提供更丰富的输出格式和分析报告。

蛋白质计算设计是一个快速发展的前沿领域，深度学习技术的不断进步为这一领域带来了前所未有的机遇。本文所开发的系统虽然存在诸多局限性，但为蛋白质Binder的计算设计提供了一个可行的技术框架和实现基础。期待未来随着数据积累、算法创新和实验验证的推进，基于深度学习的蛋白质设计能够真正成为药物研发和生物工程领域的有力工具。

---

## 参考文献

[1] Clackson T, Wells J A. A hot spot of binding energy in a hormone-receptor interface[J]. Science, 1995, 267(5196): 383-386.

[2] Kortemme T, Baker D. A simple physical model for binding energy hot spots in protein-protein complexes[J]. Proceedings of the National Academy of Sciences, 2002, 99(22): 14116-14121.

[3] Guerois R, Nielsen J E, Serrano L. Predicting changes in the stability of proteins and protein complexes: a study of more than 1000 mutations[J]. Journal of Molecular Biology, 2002, 320(2): 369-387.

[4] Darnell S J, Page D, Mitchell J C. An automated decision-tree approach to predicting protein interaction hot spots[J]. Proteins: Structure, Function, and Bioinformatics, 2007, 68(4): 813-823.

[5] Cho K, Kim D, Lee D. A feature-based approach to modeling protein-protein interaction hot spots[J]. Nucleic Acids Research, 2009, 37(8): 2672-2687.

[6] Lise S, Buchan D, Pontil M, et al. Predictions of hot spot residues at protein-protein interfaces using support vector machines[J]. PLoS ONE, 2011, 6(2): e16745.

[7] Chen R, Liu Y, Li Z, et al. PPI-hotspotID: A method for detecting protein-protein interaction hot spots from the free protein structure[J]. eLife, 2020, 9: e55215.

[8] Rives A, Meier J, Sercu T, et al. Biological structure and function emerge from scaling unsupervised learning to 250 million protein sequences[J]. Proceedings of the National Academy of Sciences, 2021, 118(15): e2016239118.

[9] Lin Z, Akin H, Rao R, et al. Evolutionary-scale prediction of atomic-level protein structure with a language model[J]. Science, 2023, 379(6637): 1123-1130.

[10] Velickovic P, Cucurull G, Casanova A, et al. Graph Attention Networks[C]. International Conference on Learning Representations (ICLR), 2018.

[11] Kipf T N, Welling M. Semi-Supervised Classification with Graph Convolutional Networks[C]. International Conference on Learning Representations (ICLR), 2017.

[12] Hu J, Shen L, Sun G. Squeeze-and-Excitation Networks[C]. IEEE Conference on Computer Vision and Pattern Recognition (CVPR), 2018: 7132-7141.

[13] Watson J L, Juergens D, Bennett N R, et al. De novo design of protein structure and function with RFdiffusion[J]. Nature, 2023, 620(7976): 1089-1100.

[14] Dauparas J, Anishchenko I, Bennett N, et al. Robust deep learning-based protein sequence design using ProteinMPNN[J]. Science, 2022, 378(6615): 49-56.

[15] Baek M, McHugh R, Anishchenko I, et al. Accurate prediction of protein-nucleic acid complexes using RoseTTAFoldNA[J]. Nature Methods, 2024, 21(1): 117-121.

[16] Jumper J, Evans R, Pritzel A, et al. Highly accurate protein structure prediction with AlphaFold[J]. Nature, 2021, 596(7873): 583-589.

[17] Baek M, DiMaio F, Anishchenko I, et al. Accurate prediction of protein structures and interactions using a three-track neural network[J]. Science, 2021, 373(6557): 871-876.

[18] Lin T Y, Goyal P, Girshick R, et al. Focal Loss for Dense Object Detection[C]. IEEE International Conference on Computer Vision (ICCV), 2017: 2980-2988.

[19] Vaswani A, Shazeer N, Parmar N, et al. Attention is all you need[C]. Advances in Neural Information Processing Systems (NeurIPS), 2017: 5998-6008.

[20] Berman H M, Westbrook J, Feng Z, et al. The Protein Data Bank[J]. Nucleic Acids Research, 2000, 28(1): 235-242.

[21] Cock P J A, Antao T, Chang J T, et al. Biopython: freely available Python tools for computational molecular biology and bioinformatics[J]. Bioinformatics, 2009, 25(11): 1422-1423.

[22] Wang M, Zheng D, Ye Z, et al. Deep Graph Library: A Graph-Centric, Highly-Performant Package for Graph Neural Networks[J]. arXiv preprint arXiv:1909.01315, 2019.

[23] Szegedy C, Vanhoucke V, Ioffe S, et al. Rethinking the Inception Architecture for Computer Vision[C]. IEEE Conference on Computer Vision and Pattern Recognition (CVPR), 2016: 2818-2826.

[24] Loshchilov I, Hutter F. Decoupled Weight Decay Regularization[C]. International Conference on Learning Representations (ICLR), 2019.

[25] Sehnal D, Bittrich S, Deshpande M, et al. Mol* Viewer: modern web app for 3D visualization and analysis of large biomolecular structures[J]. Nucleic Acids Research, 2021, 49(W1): W431-W437.

---

## 致谢

本论文的完成离不开多方面的帮助和支持。在此，谨向所有在论文研究过程中给予我指导和帮助的人表示诚挚的感谢。

首先，衷心感谢我的毕业论文指导老师。在整个毕业设计过程中，老师在选题方向、技术方案讨论、实验设计和论文撰写等各个阶段都给予了耐心细致的指导。老师严谨的治学态度、深厚的专业素养和对前沿科学问题的敏锐洞察力，使我受益匪浅。在遇到技术难题和研究瓶颈时，老师的鼓励和建议帮助我找到了突破的方向。

其次，感谢各位任课老师和实验室的师兄师姐们。本科阶段所学的专业课程为本研究的开展提供了扎实的理论基础。实验室的师兄师姐们在实验技术、代码调试和数据分析方面提供了许多宝贵的建议和帮助，使我能够更快地熟悉蛋白质设计领域的研究方法和工具。

感谢开源社区的贡献者们。rc-foundry、ESM-2、PyTorch、DGL、Molstar、FastAPI、React等开源项目和框架的存在，使得本研究的技术实现成为可能。特别感谢RosettaCommons和Meta AI等研究机构公开其研究成果和模型权重。

感谢我的家人和朋友。在漫长的毕业设计过程中，你们一如既往的理解、支持和鼓励是我不断前行的动力源泉。

最后，感谢百忙之中评阅本论文和参加答辩的各位老师和专家，你们的宝贵意见和建议将帮助我发现不足，进一步提升研究工作的质量。

---

## 附录

### 附录A 主要模型训练参数配置

| 参数 | 值 | 说明 |
|------|-----|------|
| ESM2_MODEL | facebook/esm2_t33_650M_UR50D | ESM-2预训练模型版本 |
| ESM2_DIM | 1280 | ESM-2嵌入维度 |
| MAP_CUTOFF | 10.0 Å | 图边构建距离阈值 |
| HIDDEN_DIM | 64 | GAT隐藏层维度 |
| NUM_HEADS | 4 | 多头注意力头数 |
| NUM_LAYERS | 3 | GAT层数 |
| DROPOUT | 0.5 | Dropout比率 |
| LEARNING_RATE | 1e-3 | 初始学习率 |
| NUM_EPOCHS | 200 | 最大训练轮数 |
| PATIENCE | 15 | 早停耐心值 |
| FOCAL_ALPHA | 0.7 | Focal Loss正样本权重 |
| FOCAL_GAMMA | 2.0 | Focal Loss聚焦参数 |
| LABEL_SMOOTHING | 0.05 | 标签平滑因子 |
| N_FOLDS | 5 | 交叉验证折数 |
| RANDOM_SEED | 42 | 随机种子 |

### 附录B 数据集处理代码核心逻辑

数据标准化处理的核心逻辑如下：

1. 读取原始Excel文件，解析突变记录和ΔΔG值。
2. 以ΔΔG ≥ 2.0 kcal/mol为阈值标注热点残基。
3. 建立UniProt编号到PDB编号的映射关系。
4. 从PDB文件中提取结构信息和残基坐标。
5. 对data2中的重复记录进行去重处理。
6. 输出标准化后的CSV文件供后续训练和评估使用。

### 附录C 系统主要目录结构

```
foundry-production/
├── hotspot-prediction/          # 热点预测核心模块
│   ├── config.py                # 全局配置
│   ├── dataset.py               # 数据处理与图构建
│   ├── model.py                 # 模型定义
│   ├── train.py                 # 训练循环
│   ├── src/hotspot_prediction/  # 模块化重构代码
│   ├── scripts/                 # 可执行脚本
│   ├── tests/                   # 单元测试
│   ├── data/                    # 原始数据与特征
│   ├── models/                  # 训练好的模型权重
│   ├── results/                 # 评估结果
│   └── logs/                    # 训练日志
├── binder-design-system/        # Web应用
│   ├── frontend/                # React前端
│   ├── backend/                 # FastAPI后端
│   └── utils/                   # 计算引擎适配器
├── foundry-production/          # rc-foundry框架
│   ├── src/foundry/             # 核心框架
│   └── models/                  # RFD3/MPNN/RF3模型
└── docs/                        # 项目文档
```
