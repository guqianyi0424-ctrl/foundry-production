# 基于深度学习的蛋白质Binder生成系统设计与实现

## 摘要

蛋白质Binder能够与特定靶标蛋白形成稳定结合，在蛋白质药物研发、疾病靶点干预和生物检测等场景中具有重要应用价值。传统Binder设计通常依赖人工经验和实验筛选，存在设计周期长、实验成本高、结果管理分散等问题。近年来，蛋白质语言模型、扩散生成模型和结构预测模型的发展，为构建自动化Binder设计系统提供了可行基础。围绕上述背景，本文面向软件工程本科毕业设计要求，设计并实现了一个基于深度学习的蛋白质Binder生成系统DeepBinder，重点解决目标蛋白结构上传、热点残基辅助选择、Binder骨架生成、序列设计、结构验证、实验记录管理和结果可视化等系统实现问题。

本文首先分析了蛋白质Binder设计的业务流程和系统需求，明确了研究人员、管理员和模型计算服务三类参与者。随后采用前后端分离架构设计系统，前端基于React、TypeScript、Zustand和Molstar实现结构上传、序列选择、热点标注和三维结构交互展示；后端基于FastAPI、SQLAlchemy和SQLite实现用户认证、实验管理、模型适配、任务编排和结果归档。系统在计算层集成热点残基预测、RFD3骨架生成、MPNN序列设计和RF3结构验证，其中热点预测模块采用ESM-2残基嵌入与图注意力网络进行候选热点排序；Binder设计模块在后端执行完整流水线，并将RFD3生成的多个backbone拆分为独立MPNN任务，再将每条候选序列拆分为独立RF3验证任务，通过进程内任务调度器和GPU槽位限制实现可控并行，最终依据pLDDT、RMSD和ranking_score聚合排序候选结果。系统还实现了实验记录、权限控制、导出报告、系统监控和前端预览式手动流程，确保前端过程展示不影响后端正式实验数据。

在测试方面，本文对系统进行了后端契约测试、服务编排测试、安全鲁棒性测试、前端验收测试和构建验证。热点残基预测实验表明，ESM2-GAT模型在独立测试集上取得ROC-AUC 0.7093、PR-AUC 0.0886，在Top-3、Top-5和Top-10候选推荐中的命中率分别为38.33%、43.33%和48.33%，说明其适合作为候选热点排序工具。系统测试结果表明，DeepBinder能够完成从目标蛋白输入到Binder候选结果展示的端到端流程，满足软工类毕业设计对需求分析、系统设计、系统实现和系统测试的要求。

**关键词**：蛋白质Binder；深度学习；热点残基预测；FastAPI；React；任务调度

---

## Abstract

Protein binders can specifically bind to target proteins and have important application value in protein drug development, target intervention, and biological detection. Traditional binder design usually depends on expert experience and experimental screening, which leads to long development cycles, high experimental costs, and scattered result management. With the development of protein language models, diffusion-based generative models, and structure prediction models, it has become feasible to build an automated binder design system. Based on this background, this thesis designs and implements DeepBinder, a deep learning-based protein binder generation system, with emphasis on software engineering implementation, including target structure upload, hotspot-assisted selection, binder backbone generation, sequence design, structure validation, experiment management, and visualization.

This thesis first analyzes the business workflow and requirements of protein binder design, and identifies three types of actors: researchers, administrators, and model computation services. A front-end and back-end separated architecture is adopted. The front end is implemented with React, TypeScript, Zustand, and Molstar, providing structure upload, sequence selection, hotspot annotation, and interactive 3D visualization. The back end is implemented with FastAPI, SQLAlchemy, and SQLite, providing authentication, experiment management, model adapters, task orchestration, and result archiving. At the computation layer, the system integrates hotspot residue prediction, RFD3 backbone generation, MPNN sequence design, and RF3 structure validation. The hotspot prediction module uses ESM-2 residue embeddings and a graph attention network for candidate hotspot ranking. The binder design module executes a complete backend-owned pipeline: multiple backbones generated by RFD3 are expanded into independent MPNN tasks, and each candidate sequence is further expanded into independent RF3 validation tasks. An in-process task scheduler with GPU slot limits is used for controlled parallel execution, and the final candidates are aggregated and ranked by pLDDT, RMSD, and ranking_score. The system also supports experiment records, role-based access control, report export, system monitoring, and preview-only manual frontend steps that do not affect official backend data.

For evaluation, backend contract tests, service orchestration tests, security robustness tests, frontend acceptance tests, and build verification were conducted. The hotspot prediction experiment shows that the ESM2-GAT model achieves ROC-AUC 0.7093 and PR-AUC 0.0886 on the independent test set, with Top-3, Top-5, and Top-10 hit rates of 38.33%, 43.33%, and 48.33%, respectively. These results indicate that the model is more suitable for hotspot candidate ranking than for high-confidence binary classification. System testing results demonstrate that DeepBinder can complete the end-to-end workflow from target protein input to binder candidate visualization, meeting the requirements of a software engineering undergraduate graduation project.

**Keywords**: Protein binder; Deep learning; Hotspot residue prediction; FastAPI; React; Task scheduling

---

## 目录

- 第1章 绪论
- 第2章 相关技术
- 第3章 需求分析
- 第4章 系统设计
- 第5章 系统实现
- 第6章 系统测试
- 第7章 总结与展望
- 参考文献
- 致谢
- 附录

---

## 第1章 绪论

### 1.1 研究背景与意义

蛋白质是生命活动中的重要功能分子，细胞信号传递、免疫识别、代谢调控和病原体感染等过程均与蛋白质之间的相互作用密切相关。蛋白质-蛋白质相互作用（Protein-Protein Interaction，PPI）通常发生在特定结合界面上，界面中的少数关键残基会对结合自由能产生较大贡献，这些残基通常被称为热点残基。准确识别热点残基有助于理解蛋白质相互作用机制，也能为蛋白质工程和Binder设计提供候选作用区域。

Binder蛋白是指能够与目标蛋白发生特异性结合的蛋白质分子。高质量Binder可用于阻断疾病相关PPI、构建蛋白质药物、实现靶向递送或辅助生物检测。传统Binder设计依赖噬菌体展示、酵母展示、定向进化等实验方法，通常需要进行大量候选分子的实验筛选。该流程虽然可靠，但存在周期长、成本高、对实验条件依赖强等问题。随着深度学习方法在蛋白质结构预测和蛋白质设计领域的发展，研究人员开始利用计算模型生成Binder骨架、设计序列并预测复合物结构，从而缩短早期候选筛选周期。

近年来，蛋白质语言模型ESM-2能够从大量蛋白质序列中学习残基级表示；RFDiffusion及其后续版本能够根据结构条件生成蛋白质骨架；ProteinMPNN能够在给定骨架下设计氨基酸序列；RoseTTAFold系列模型能够对设计结构进行预测与验证。这些模型为构建端到端Binder生成系统提供了技术基础。然而，在实际使用中，这些工具通常以命令行脚本、独立模型仓库或实验性接口形式存在，使用门槛较高，结果管理分散，缺少面向完整业务流程的Web系统支撑。对于软件工程专业毕业设计而言，将这些深度学习模型封装为可用、可测试、可管理的软件系统，比单独改进某一个算法模型更符合工程实践要求。

基于上述背景，本文设计并实现了DeepBinder蛋白质Binder生成系统。系统以目标蛋白结构为输入，提供结构解析、热点残基预测、靶标范围选择、RFD3骨架生成、MPNN序列设计、RF3结构验证、实验记录管理和结果可视化等功能。本文的意义主要体现在以下三个方面。

第一，系统将热点残基预测和Binder设计流程结合起来，帮助用户从目标蛋白结构出发获得候选Binder结果，降低分散使用多个模型工具的复杂度。第二，系统采用前后端分离、模型适配器、服务编排和任务调度等软件工程方法，将深度学习模型封装为可交互Web应用，体现了软工专业对系统分析、设计、实现和测试的训练目标。第三，系统在后端执行完整pipeline，并通过并行任务调度提高多backbone、多序列验证场景下的资源利用率，为后续多GPU或云端部署提供扩展基础。

【图1-1 课题应用背景示意图：建议绘制“目标蛋白结构 -> 热点区域 -> Binder生成 -> 候选结果验证”的流程图。可使用PowerPoint、draw.io或ProcessOn绘制。】

### 1.2 国内外研究现状

#### 1.2.1 热点残基预测研究现状

热点残基预测最早主要依赖实验突变扫描和物理能量函数。丙氨酸扫描突变通过将界面残基替换为丙氨酸，并检测结合自由能变化来识别热点残基。若突变导致结合自由能变化达到一定阈值，通常认为该残基对结合具有重要贡献。该方法准确性较高，但实验成本高，不适合大规模筛选。基于物理能量函数的方法通过模拟突变前后能量变化来估计残基重要性，具有一定可解释性，但容易受到结构质量、力场参数和溶剂模型影响。

传统机器学习方法通常将氨基酸类型、溶剂可及表面积、保守性、二级结构、能量项等人工特征输入支持向量机、随机森林或集成学习模型。PPI-hotspotID等方法综合利用多种序列和结构特征，在非抗体PPI热点残基预测任务中具有代表性。这类方法的优点是能够利用明确的生物物理特征，在小规模数据集上较为稳健；不足是特征工程复杂，依赖多个外部工具，预处理链路较长。

深度学习方法为热点残基预测提供了新的思路。蛋白质语言模型ESM系列通过大规模序列预训练学习残基级上下文表示，能够在不重新执行多序列比对的情况下提供高维特征。图神经网络适合处理蛋白质三维结构，因为蛋白质可以自然表示为残基节点和空间邻接边构成的图。图注意力网络（Graph Attention Network，GAT）能够自适应聚合邻域残基信息，有助于捕捉PPI界面中的局部结构模式。本文的热点预测模块采用ESM-2嵌入与GAT结构，将其定位为Binder设计前的候选热点排序工具。

#### 1.2.2 蛋白质Binder设计研究现状

蛋白质设计任务一般包括骨架生成、序列设计和结构验证。早期方法多依赖Rosetta等基于能量函数的计算设计框架，通过采样、能量优化和人工筛选生成候选结构。近年来，深度生成模型显著提高了蛋白质设计的自动化程度。

RFDiffusion将扩散模型用于蛋白质骨架生成，在特定结构条件下可以生成满足约束的蛋白质骨架。ProteinMPNN将蛋白质骨架表示为图结构，在给定主链几何的条件下预测合适的氨基酸序列，其计算效率和序列恢复能力优于传统固定骨架设计方法。RoseTTAFold和AlphaFold系列模型则为设计结果提供结构预测与可信度评估，使计算设计流程可以在进入实验验证前完成初步筛选。

尽管上述模型能力较强，但真实使用时仍存在工程化问题。例如，RFD3、MPNN和RF3通常有不同的输入输出格式、运行环境和文件组织方式；单次设计会生成多个backbone，每个backbone又可能对应多条候选序列，如果只串行执行会浪费计算资源；如果前端手动触发各阶段并直接写入正式实验数据，又容易造成结果来源不清。本文系统将模型调用封装为后端适配器，由后端统一运行完整pipeline，并通过任务调度器对MPNN和RF3阶段进行受控并行，从系统层面解决模型集成和结果管理问题。

#### 1.2.3 Web化生物信息系统研究现状

生物信息学工具逐渐从命令行脚本转向Web系统和云平台。Web系统的优势在于交互门槛低、结果易管理、可与可视化工具结合，并可通过权限控制和任务记录支持团队协作。蛋白质结构类系统尤其需要三维可视化能力，Molstar等现代WebGL分子查看器能够在浏览器中展示PDB/CIF结构，并支持残基选择、高亮、颜色映射和结构叠加。

现有蛋白质设计模型多以研究工具形式发布，对于非计算背景用户并不友好。本文以软件工程方式构建DeepBinder系统，通过React前端提供结构上传、序列视图和Molstar三维交互，通过FastAPI后端提供RESTful接口、认证、实验记录和模型服务编排，使深度学习Binder设计流程具有更好的可用性和可维护性。

### 1.3 论文研究内容

本文围绕“基于深度学习的蛋白质Binder生成系统设计与实现”展开，重点是系统实现，主要工作如下。

（1）完成DeepBinder系统需求分析。结合Binder设计实际流程，分析研究人员、管理员和模型计算服务的角色，明确结构上传、热点预测、靶标范围选择、完整pipeline运行、手动过程展示、实验记录管理、用户权限和系统监控等功能需求。

（2）设计前后端分离的软件架构。前端采用React、TypeScript、Zustand和Molstar实现用户交互和结构可视化；后端采用FastAPI、SQLAlchemy、SQLite、Pydantic和JWT实现业务接口、数据持久化和权限控制；模型层通过Adapter模式封装热点预测、RFD3、MPNN和RF3。

（3）实现后端主导的Binder设计pipeline。系统后端运行完整RFD3->MPNN->RF3流程，RFD3批量生成多个backbone后，系统将每个backbone拆分为MPNN子任务，再将每条候选序列拆分为RF3验证任务，通过进程内线程池调度和GPU槽位参数限制并发，最终按pLDDT、RMSD和ranking_score聚合排序候选结果。

（4）实现热点残基预测子模块。基于热点预测初稿中的实验流程，采用ESM-2残基嵌入和GAT模型进行热点残基候选排序，并将预测结果用于前端红点标注和设计参数辅助输入。该模块不作为论文唯一重点，而作为系统的智能辅助功能。

（5）实现系统测试与验证。通过后端服务测试、路由契约测试、安全鲁棒性测试、前端验收测试和构建测试验证系统可用性；通过热点预测实验数据说明热点预测模块具有候选推荐价值。

### 1.4 论文组织结构

本文共分为七章。

第1章为绪论，介绍课题背景、研究意义、国内外研究现状、本文研究内容和论文结构。第2章为相关技术，介绍蛋白质Binder设计涉及的深度学习模型、Web开发技术和任务调度技术。第3章为需求分析，从业务需求、功能需求和非功能需求三个角度分析系统。第4章为系统设计，阐述总体架构、功能模块、数据库、API接口和任务调度方案。第5章为系统实现，重点描述前端、后端、模型适配、并行pipeline和实验管理等模块的实现。第6章为系统测试，给出热点预测实验、后端测试、前端验收测试和系统构建验证结果。第7章为总结与展望，总结本文工作并提出后续改进方向。

---

## 第2章 相关技术

### 2.1 蛋白质Binder与热点残基

蛋白质Binder是能够与目标蛋白形成稳定相互作用的蛋白质分子。一个理想Binder通常需要满足结构稳定、结合界面合理、与目标蛋白具有较高亲和力和特异性等条件。计算Binder设计一般从目标蛋白结构出发，确定可能结合区域，然后生成能够贴合该区域的蛋白质骨架，再为骨架设计氨基酸序列，最后通过结构预测或能量评估筛选候选结果。

热点残基是PPI界面中对结合自由能贡献较大的残基。实验上常通过丙氨酸扫描突变识别热点残基，当某残基突变后导致结合自由能显著变化时，该残基可被视为热点。热点残基通常位于界面核心区域，可能具有较强保守性或形成关键氢键、盐桥和疏水接触。在Binder设计系统中，热点残基可以作为靶向区域提示，帮助用户选择设计目标区域。

### 2.2 蛋白质语言模型ESM-2

ESM-2是基于Transformer结构的蛋白质语言模型，通过大规模蛋白质序列自监督训练获得残基级表示。蛋白质序列可被视为由氨基酸字母组成的“语言”，序列中包含结构、功能和进化信息。ESM-2通过掩码语言建模任务学习氨基酸上下文关系，并为每个残基输出高维嵌入向量。

在本文系统中，ESM-2主要用于热点残基预测模块。系统从PDB结构中提取目标链序列，将序列输入ESM-2获取残基嵌入，再将嵌入作为图神经网络节点特征。与传统PSSM、HMM等特征相比，ESM-2特征无需针对每条序列重新执行多序列比对，工程集成更简洁，适合Web系统中的模型服务封装。

### 2.3 图注意力网络

图神经网络适合处理蛋白质结构数据。蛋白质可表示为残基图，其中节点表示氨基酸残基，边表示残基间的空间邻近关系。图神经网络通过消息传递机制将邻居残基信息聚合到目标节点上，使模型能够学习局部结构环境。

图注意力网络（GAT）在消息传递过程中引入注意力机制，为不同邻居分配不同权重。对于热点残基预测任务，某些邻域残基可能对结合界面贡献更大，GAT能够根据节点特征和图结构自适应学习邻域重要性。本文的热点预测模型采用ESM-2嵌入作为节点特征，以残基距离构建边，通过GAT输出每个残基为热点的概率。

【图2-1 ESM-2与GAT热点预测流程图：输入PDB/序列 -> ESM-2残基嵌入 -> 残基图构建 -> GAT消息传递 -> 热点概率输出。建议根据 `热点残基预测论文初稿.md` 中图2-1要求绘制。】

### 2.4 RFD3、MPNN与RF3

RFD3用于Binder骨架生成。其思想来源于蛋白质结构扩散模型，通过从噪声结构逐步去噪，生成满足目标条件的蛋白质骨架。在Binder设计场景中，目标蛋白结构、靶标残基范围和热点残基约束可以作为条件，引导模型生成能够与目标区域形成接触的Binder骨架。

MPNN用于固定骨架序列设计。其输入是蛋白质主链骨架结构，输出是能够稳定该骨架的氨基酸序列。对于RFD3生成的每个backbone，MPNN可以生成多条候选序列，以提高获得优质设计的概率。

RF3用于结构验证。系统将MPNN输出的候选序列与目标蛋白结构输入RF3，获得预测复合物结构以及pLDDT、pTM、iPTM、PAE、RMSD等指标。pLDDT越高通常表示局部结构预测越可靠，RMSD越低通常表示预测结构与参考结构更接近。本文系统最终根据pLDDT、RMSD和ranking_score对候选Binder进行综合排序。

### 2.5 FastAPI与后端服务技术

FastAPI是基于Python类型注解的Web框架，具有自动参数校验、OpenAPI文档生成和异步支持等特点。由于DeepBinder需要调用Python生态中的深度学习模型，后端采用FastAPI可以减少跨语言调用复杂度。系统后端使用Pydantic定义请求数据结构，使用SQLAlchemy访问SQLite数据库，使用JWT进行用户身份认证。

本文后端采用Router、Service、Adapter、Repository分层组织代码。Router负责HTTP接口；Service负责业务编排；Adapter负责模型调用封装；Repository负责数据库访问。该结构能够降低模块耦合，使模型运行逻辑和Web接口逻辑分离。

### 2.6 React、Zustand与Molstar

React是前端组件化开发框架，适合构建交互复杂的单页应用。TypeScript为前端代码提供类型约束，能够减少接口字段变化导致的运行时错误。Zustand是轻量级状态管理库，本文系统用其保存用户登录状态、PDB内容、链信息、选中残基范围、热点残基、RFD3结果、MPNN结果和RF3结果等全局状态。

Molstar是面向生物大分子结构的Web可视化组件，能够在浏览器中渲染PDB/CIF结构，并支持旋转、缩放、选择和高亮。DeepBinder前端通过Molstar展示目标蛋白结构、用户选中的靶标范围、预测热点残基和RF3验证结构，使用户能够直观理解设计过程。

### 2.7 本章小结

本章介绍了DeepBinder系统涉及的关键技术。蛋白质Binder和热点残基构成系统的业务基础；ESM-2和GAT为热点预测提供模型支撑；RFD3、MPNN和RF3分别承担骨架生成、序列设计和结构验证任务；FastAPI、React、Zustand、Molstar和SQLite构成系统的软件实现基础。这些技术共同支撑了后续章节中的需求分析、系统设计和系统实现。

---

## 第3章 需求分析

### 3.1 系统总体需求

DeepBinder系统面向蛋白质工程和生物信息学研究人员，目标是提供一个从目标蛋白结构输入到Binder候选结果输出的一站式Web工具。系统需要解决以下实际问题。

第一，用户需要上传目标蛋白结构文件，并查看其中的链、残基和三维结构。第二，用户需要在序列视图和三维结构视图中选择靶标范围或热点残基。第三，系统需要能够运行热点残基预测，给出候选关键残基并以可视化方式标注。第四，系统需要运行后端完整Binder设计pipeline，而不是依赖前端逐步处理数据。第五，系统需要保存实验记录和设计结果，支持后续查看、导出和对比。第六，系统需要提供登录、权限和管理员监控能力，以满足多用户环境下的数据安全要求。

系统典型业务流程如下：研究人员登录系统后上传目标蛋白PDB文件；系统解析链和残基信息，并在Molstar中显示三维结构；用户可运行热点预测或手动选择靶标范围；用户配置RFD3生成参数并启动完整pipeline；后端依次执行RFD3、MPNN和RF3，并按结果指标排序；用户在实验详情页查看设计候选、下载结果报告或继续进行前端预览式过程展示。

【图3-1 系统业务流程图：用户登录 -> 上传结构 -> 解析与可视化 -> 预测/选择热点 -> 配置设计参数 -> 后端运行完整pipeline -> 结果排序与实验记录。】

### 3.2 系统参与者分析

系统主要参与者包括研究人员、管理员和模型计算服务。

研究人员是系统主要使用者，负责上传目标蛋白结构、选择设计区域、启动设计任务、查看和导出实验结果。研究人员只能访问自己创建的实验记录，不能查看或修改其他用户数据。

管理员负责系统维护和用户管理。管理员可以查看系统监控信息、管理用户列表、按用户筛选实验记录，并处理系统运行过程中的资源或权限问题。

模型计算服务是系统内部参与者，包括热点预测服务、RFD3适配器、MPNN适配器和RF3适配器。它们不直接与前端交互，而由后端服务统一调度。

【图3-2 系统用例图：参与者包括研究人员、管理员、模型计算服务；用例包括登录、上传结构、热点预测、运行pipeline、查看实验记录、导出结果、用户管理、系统监控。】

### 3.3 功能性需求

根据系统业务流程，DeepBinder主要功能需求如下。

（1）用户认证与权限管理。系统应支持用户注册、登录、退出和当前用户信息查询；用户登录后获得JWT访问令牌；管理员与研究人员具有不同权限；匿名用户不能访问需要登录的实验接口；研究人员不能访问其他用户的实验数据。

（2）结构上传与解析。系统应支持上传PDB结构文件，检查文件格式、空文件和大小限制；上传成功后返回链信息、链序列和原始PDB内容；前端应能够显示链、残基序列和三维结构。

（3）热点残基预测与标注。系统应接收PDB内容并调用热点预测服务，返回Top-K热点残基、得分、方法和模型加载状态。前端应在序列视图下方用红点标记预测热点，用户可双击移除预测热点，也可手动选择热点。预测热点只有在用户点击“指定热点”后才写入正式RFD3配置。

（4）靶标范围选择。前端应支持用户在序列视图中拖拽选择残基范围，并在用户点击“裁剪靶点”后将该范围固定到RFD3配置中的目标实体字段。固定后的靶标范围在界面中以“固定靶标”状态提示，后续拖拽只更新当前选择范围，不会覆盖已固定的正式靶标配置。Molstar视图应持久高亮用户选择的靶标范围和热点残基。

（5）完整Binder设计pipeline。系统后端应提供`POST /api/run-pipeline`接口，接收PDB内容、靶标范围、热点残基、Binder长度、batch数量、任务名等参数，并由后端自行运行RFD3、MPNN和RF3完整流程。正式实验数据以该接口输出为准，前端手动点击MPNN/RF3仅用于展示过程，不写入正式实验记录。

（6）并行任务调度。RFD3生成多个backbone后，系统应将每个backbone的MPNN设计拆分为独立任务；MPNN生成多条候选序列后，系统应将每条序列的RF3验证拆分为独立任务；任务执行受GPU槽位参数控制，避免资源过载。

（7）实验记录管理。系统应保存实验基本信息、RFD3配置与结果、MPNN配置与结果、RF3配置与结果、候选设计序列、PDB内容、pLDDT、RMSD、ranking_score和验证状态。用户可分页查看实验列表、查看详情、添加设计结果、导出JSON报告和CSV候选列表。

（8）系统监控和用户管理。管理员可查看CPU、内存、磁盘、GPU和最近任务信息，其中GPU监控应展示设备名称、显存占用和利用率，并在不同运行环境下提供可用的采集回退；管理员可查看用户列表，并按用户查看实验记录。

### 3.4 非功能性需求

（1）可用性需求。系统界面应围绕Binder设计流程组织，用户进入系统后能够直接看到新建设计工作台，而不是营销式首页。主要操作包括上传结构、选择靶标、运行pipeline和查看结果。错误提示应使用用户可理解的描述，避免暴露底层堆栈。

（2）性能需求。结构上传和解析应快速返回；热点预测和模型pipeline属于长耗时任务，应通过后端任务编排和前端运行状态提示避免用户误操作。对于RFD3、MPNN、RF3等模型推理，系统应支持通过环境变量配置并发槽位，使单GPU和多GPU环境均可部署。

（3）安全性需求。系统应使用JWT认证；实验记录需进行对象级权限控制；管理员接口需要管理员角色；注册输入、上传文件和CORS来源需进行校验；登录失败应具有限流机制。

（4）可维护性需求。模型调用应通过Adapter模式封装，业务层不直接依赖模型脚本；实验数据访问通过Repository或Service封装；前端状态集中管理；测试覆盖关键接口和源码契约。

（5）可扩展性需求。系统后续应能够扩展更多模型，如结合自由能评估、免疫原性预测和稳定性评分。任务调度层应支持未来替换为Redis/Celery等外部队列，而不影响pipeline业务聚合逻辑。

### 3.5 本章小结

本章从用户角色、业务流程、功能需求和非功能需求四个方面分析了DeepBinder系统。系统不仅需要完成深度学习模型集成，还需要满足Web系统的登录权限、数据管理、可视化交互、任务调度和测试验证要求。上述需求为第4章系统设计提供了依据。

---

## 第4章 系统设计

### 4.1 总体架构设计

DeepBinder采用B/S架构和前后端分离模式。前端负责用户交互、结构可视化和结果展示；后端负责接口服务、用户认证、实验管理、模型适配和pipeline编排；数据库负责保存用户、实验、设计结果和审计日志；文件系统负责保存模型输出和归档报告。

系统逻辑上划分为表示层、接口层、业务服务层、模型适配层和数据持久层。

表示层由React前端构成，主要包括登录页、新建设计页、De Novo设计页、实验记录页、实验详情页、用户管理页、系统监控页和帮助页。接口层由FastAPI Router组成，向前端提供`/api/auth`、`/api/upload`、`/api/run-pipeline`、`/api/run-rfd3`、`/api/run-mpnn`、`/api/run-rf3`、`/api/experiments`和`/api/monitor`等接口。业务服务层包括`DesignPipelineService`、`ExperimentService`、`HotspotPredictionService`和`ExperimentArchiveService`。模型适配层包括`RFD3Adapter`、`MPNNAdapter`、`RF3Adapter`和`HotspotModelAdapter`。数据持久层由SQLite数据库和输出目录组成。

```text
React + TypeScript + Molstar
        |
        | Axios /api
        v
FastAPI Routers
        |
        v
Services: pipeline / experiments / hotspot / archive
        |
        v
Adapters: RFD3 / MPNN / RF3 / Hotspot
        |
        v
SQLite + FileSystem + Model Runners
```

【图4-1 系统总体架构图：建议根据上面的分层结构绘制正式架构图。图中标出React、FastAPI、Services、Adapters、SQLite、RFD3/MPNN/RF3/Hotspot模型。】

### 4.2 功能模块设计

系统功能模块包括用户认证模块、结构处理模块、热点预测模块、Binder设计模块、任务调度模块、实验管理模块、可视化模块和系统管理模块。

用户认证模块负责注册、登录、Token验证和角色识别。研究人员与管理员具有不同访问权限。结构处理模块负责上传PDB文件、解析链和残基、返回PDB内容和链序列。热点预测模块负责调用热点模型，返回候选热点，并在前端提供候选标注。Binder设计模块负责后端完整pipeline，是系统核心模块。任务调度模块负责MPNN和RF3阶段的并行调度。实验管理模块负责实验数据增删改查、报告导出和候选结果保存。可视化模块负责Molstar三维展示、序列视图、热点红点和结果卡片。系统管理模块负责管理员用户列表和系统监控。

【图4-2 系统功能模块图：顶层为DeepBinder，下层分为认证、结构处理、热点预测、Binder设计、任务调度、实验管理、可视化、系统管理八个模块。】

### 4.3 后端完整Pipeline设计

本文系统设计的核心是后端主导的完整pipeline。旧式做法是前端先运行RFD3，再由用户手动点击将单个结果送入MPNN，再将某条序列送入RF3，这种方式适合演示过程，但不适合作为正式实验数据来源。正式实验需要由后端统一执行完整流程，并保证所有候选结果来源一致。因此，DeepBinder将`/api/run-pipeline`作为正式实验入口。

后端pipeline流程如下。

（1）创建实验记录。系统根据任务名、PDB内容、热点残基、目标范围和RFD3参数创建`Experiment`记录，状态设为running。

（2）执行RFD3。系统将PDB内容、目标范围、热点残基、长度范围、batch数量等封装为`RFD3JobConfig`并调用RFD3适配器。RFD3返回多个backbone设计结果，系统保存RFD3配置和结果。

（3）执行MPNN fan-out。系统从RFD3结果中提取所有backbone，每个backbone形成一个MPNN任务。任务输入包括backbone PDB内容、固定链信息、batch_size和模型类型。任务通过`PipelineTaskScheduler`按槽位并发执行。

（4）执行RF3 fan-out。系统从所有成功MPNN任务中提取候选序列，每条序列形成一个RF3验证任务。RF3任务输入包括候选序列PDB内容、对应backbone PDB内容、example_id和job_id。任务同样受RF3槽位限制。

（5）聚合排序。系统将RF3验证结果映射回对应backbone和sequence。若某条候选RF3成功，则候选的pLDDT和ranking_score来源于RF3；若失败，则保留MPNN序列和RFD3 fallback信息，并标记validation_status为failed。最终候选按pLDDT降序、RMSD升序、ranking_score降序排序。

（6）保存与归档。系统将聚合后的候选结果写入`ExperimentDesign`，更新实验状态为completed或failed，并按需生成归档报告。

【图4-3 后端完整Pipeline时序图：前端调用run-pipeline -> 后端创建实验 -> RFD3 -> MPNN并行任务组 -> RF3并行任务组 -> 聚合排序 -> 保存实验与候选。】

### 4.4 任务调度设计

本文选择进程内任务调度作为当前实现方案。原因是系统目前部署形态以单机或单服务器为主，引入Redis/Celery会增加部署复杂度；而MPNN和RF3阶段的并行边界清晰，适合先用轻量线程池实现。调度器设计为`PipelineTaskScheduler`，提供`run_mpnn_tasks`和`run_rf3_tasks`两个方法。内部使用`ThreadPoolExecutor`，并按槽位数限制最大并发。

槽位配置通过环境变量读取：

```text
DEEPBINDER_PIPELINE_MPNN_SLOTS=1
DEEPBINDER_PIPELINE_RF3_SLOTS=1
```

默认值均为1，确保单GPU环境下不会过载。若部署在多GPU或多进程资源环境，可将对应槽位调大。当前系统仍采用进程内执行器完成模型计算，以降低单机部署复杂度；同时为异步pipeline增加了作业状态持久化，作业ID、运行状态、阶段性结果、错误信息和关联实验ID会写入数据库，前端可在页面切换后继续轮询作业状态。未来若需要跨机器调度，可将执行器替换为Redis/Celery等外部队列，并保留现有作业查询接口作为前端契约。

【图4-4 MPNN/RF3任务拆分与槽位调度图：RFD3输出Backbone1...BackboneN；每个Backbone进入MPNN任务队列；MPNN输出Sequence；每条Sequence进入RF3任务队列；两个队列分别受slots限制。】

### 4.5 数据库设计

系统采用SQLite数据库，主要表包括`users`、`experiments`、`experiment_designs`、`pipeline_jobs`和`audit_logs`。

`users`表保存用户信息，包括用户ID、用户名、邮箱、密码哈希、角色、创建时间和最近登录时间。角色字段用于区分管理员和研究人员。

`experiments`表保存实验主记录，包括实验ID、名称、状态、创建时间、更新时间、输入PDB、目标范围、热点残基、RFD3/MPNN/RF3配置、RFD3/MPNN/RF3结果、耗时、GPU信息和用户ID。该表是实验记录页和实验详情页的主要数据来源。

`experiment_designs`表保存候选设计结果，包括设计ID、实验ID、设计名称、序列、PDB内容、pLDDT、RMSD、ranking_score、是否通过验证、pLDDT来源、ranking来源和验证状态。该表用于最终候选列表、CSV导出和实验报告。

`pipeline_jobs`表保存异步pipeline作业状态，包括作业ID、状态、关联实验ID、失败阶段、错误信息、RFD3阶段结果、MPNN阶段结果、RF3阶段结果、创建时间和更新时间。该表用于前端轮询长耗时任务，支持RFD3结果先展示、MPNN/RF3继续后台运行的交互方式。

`audit_logs`表保存审计日志，包括用户ID、操作、目标、详情和时间戳，用于追踪关键操作。

表4-1给出核心数据库表。

| 表名 | 作用 | 关键字段 |
|---|---|---|
| users | 用户与角色 | id, username, email, hashed_password, role |
| experiments | 实验主记录 | id, name, status, input_pdb, target, hotspots, rfd3_results, mpnn_results, rf3_results, user_id |
| experiment_designs | 候选设计结果 | experiment_id, design_name, sequence, pdb_content, plddt, rmsd, ranking_score, validation_status |
| pipeline_jobs | 异步pipeline作业 | id, status, experiment_id, failed_step, error, rfd3_results, mpnn_results, rf3_results |
| audit_logs | 审计日志 | user_id, action, target, detail, timestamp |

【图4-5 数据库E-R图：users 1-N experiments；experiments 1-N experiment_designs；experiments 1-N pipeline_jobs；users 1-N audit_logs。】

### 4.6 API接口设计

系统主要API接口如表4-2所示。

| 模块 | 方法与路径 | 功能 |
|---|---|---|
| 认证 | POST /api/auth/register | 用户注册 |
| 认证 | POST /api/auth/login | 用户登录 |
| 认证 | GET /api/auth/me | 获取当前用户 |
| 上传 | POST /api/upload | 上传并解析PDB文件 |
| 设计 | POST /api/predict-hotspot | 热点残基预测 |
| 设计 | POST /api/run-pipeline | 后端完整Binder pipeline，支持`async_mode` |
| 设计 | GET /api/pipeline-jobs/{job_id} | 查询异步pipeline作业状态 |
| 设计 | POST /api/run-rfd3 | 手动/独立RFD3运行 |
| 设计 | POST /api/run-mpnn | 手动/预览MPNN运行 |
| 设计 | POST /api/run-rf3 | 手动/预览RF3运行 |
| 实验 | GET /api/experiments | 分页查询实验列表 |
| 实验 | GET /api/experiments/{id} | 查询实验详情 |
| 实验 | GET /api/experiments/{id}/export | 导出实验报告 |
| 实验 | GET /api/experiments/{id}/candidates.csv | 导出候选CSV |
| 监控 | GET /api/monitor/status | 管理员查看系统状态 |
| 用户 | GET /api/users | 管理员查看用户列表 |

正式设计流程使用`/api/run-pipeline`。长耗时场景下前端传入`async_mode=true`，后端先返回作业ID，再由前端通过`/api/pipeline-jobs/{job_id}`轮询进度和结果。前端手动MPNN和RF3接口使用`preview_only=true`，且不传实验ID，避免影响后端正式结果。

### 4.7 前端交互设计

前端核心页面为“新建设计”。页面左侧为导航栏，右侧为工作区。工作区包含目标结构区域、Molstar三维视图、序列选择视图、RFD3设计参数面板、RFD3结果、MPNN结果和RF3验证结果。用户可以上传PDB文件，系统解析链和残基后在序列视图中展示。热点预测结果在序列字符下方以红点标记，靶标范围在序列和三维视图中高亮。

为展示设计过程，前端仍保留“送入MPNN”和“送入RF3验证”按钮。这些按钮调用后端预览接口，只更新前端显示状态，不写入实验记录。正式实验数据以“新建任务”按钮触发的异步`runPipeline`结果为准。前端在收到RFD3阶段性结果后可先展示backbone预览，同时继续轮询MPNN和RF3阶段，减少长任务期间的空白等待。

【图4-6 新建设计页面线框图：左侧导航；中部Molstar结构视图；下方序列视图；右侧或下方RFD3参数面板；结果区域含RFD3/MPNN/RF3三个阶段。】

【图4-7 前端状态流转图：上传PDB -> setPdbContent/setChains -> 选择范围/热点 -> 裁剪靶点固定rfd3Config -> startProteinRfd3Run(async_mode) -> 轮询pipeline job -> rfd3Results/mpnnResults/rf3Results。】

### 4.8 本章小结

本章完成了DeepBinder系统设计。系统采用前后端分离架构和服务分层设计，后端通过模型适配器封装RFD3、MPNN、RF3和热点预测服务，通过`DesignPipelineService`实现完整pipeline，通过`PipelineTaskScheduler`实现MPNN和RF3阶段的可控并行。数据库设计围绕用户、实验和候选设计结果展开。前端设计强调结构可视化、热点标注和过程展示，同时确保正式数据由后端统一生成。

---

## 第5章 系统实现

### 5.1 开发环境与项目结构

DeepBinder项目位于`binder-design-system`目录下，采用前后端分离结构。后端目录为`backend`，前端目录为`frontend`，通用模型运行工具位于`utils`目录。项目还包含部署脚本、模块文档和验收测试文档。

主要开发环境如下。

| 类别 | 技术/版本 | 说明 |
|---|---|---|
| 后端语言 | Python 3.12 | FastAPI服务和模型适配 |
| 后端框架 | FastAPI | REST API、请求校验 |
| ORM | SQLAlchemy | SQLite数据库访问 |
| 前端框架 | React 18 + TypeScript | 单页Web应用 |
| 构建工具 | Vite | 前端开发与生产构建 |
| 状态管理 | Zustand | 全局设计状态与认证状态 |
| 结构可视化 | Molstar | 浏览器端PDB三维展示 |
| 数据库 | SQLite | 用户、实验和设计结果 |
| 测试 | pytest、Vitest | 后端和前端自动化测试 |

【表5-1 开发环境表：最终提交前建议补充实际服务器CPU、GPU、内存、操作系统、Node.js版本。可使用`nvidia-smi`、`python --version`、`node --version`截图或记录。】

项目关键目录如下。

```text
binder-design-system/
├── backend/
│   ├── routers/          # auth、upload、design、experiments、monitor等路由
│   ├── services/         # pipeline、experiment、hotspot、archive、scheduler服务
│   ├── adapters/         # RFD3、MPNN、RF3、热点模型适配器
│   ├── repositories/     # 实验记录数据访问
│   ├── config/           # 路径和运行时配置
│   ├── database.py       # SQLAlchemy模型
│   └── tests/            # 后端测试
├── frontend/
│   ├── src/pages/        # 新建设计、实验记录、监控等页面
│   ├── src/components/   # MolstarViewer、SequenceViewer、DesignPanel等组件
│   ├── src/store/        # Zustand状态管理
│   └── src/api/          # Axios接口封装
└── utils/                # rfd3_runner、mpnn_runner、rf3_runner、hotspot_predictor
```

### 5.2 后端服务实现

后端入口文件`main.py`创建FastAPI应用，并注册认证、上传、设计、实验、作业和监控路由。应用统一使用`/api`前缀对外提供接口。后端启动时初始化数据库；生产环境不再自动创建固定的默认管理员账号，而是通过`DEEPBINDER_BOOTSTRAP_ADMIN_USERNAME`、`DEEPBINDER_BOOTSTRAP_ADMIN_PASSWORD`和`DEEPBINDER_BOOTSTRAP_ADMIN_EMAIL`显式引导管理员，避免默认口令带来的安全风险。

后端代码按职责分层。Router层接收HTTP请求并完成请求参数校验；Service层处理业务编排；Adapter层封装模型调用；Repository层封装数据库读写。以设计流程为例，`routers/design.py`负责定义`PipelineRequest`、`RFD3Request`、`MPNNRequest`和`RF3Request`等请求模型；`services/design_pipeline.py`负责执行完整pipeline；`adapters/model_adapters.py`负责将模型runner输出统一包装为`AdapterResult`；`services/experiments.py`和`repositories/experiments.py`负责实验记录持久化。

后端接口在模型结果入库前会调用`sanitize_model_result`对PDB大字段进行脱敏，避免实验JSON结果中保存过大的结构字符串。正式候选结构内容保存在`ExperimentDesign.pdb_content`字段中，用于结构预览和导出。

### 5.3 认证与权限实现

系统认证模块采用JWT机制。用户注册时，后端对用户名、密码和邮箱进行校验，并对密码进行哈希存储。用户登录成功后，系统返回访问令牌，前端将令牌保存在`sessionStorage`中。后续请求通过Axios拦截器在请求头加入`Authorization: Bearer <token>`。

权限控制分为接口级权限和对象级权限。接口级权限用于限制匿名用户或非管理员访问，例如实验接口要求登录，系统监控接口要求管理员角色。对象级权限用于限制研究人员访问他人实验记录，实验详情、更新、删除、导出和添加设计结果等接口都会检查实验所属用户。如果当前用户不是管理员且实验不属于该用户，后端返回403。

【图5-1 登录与权限校验时序图：用户登录 -> 返回token -> 前端保存token -> 请求实验详情 -> 后端解析token -> 检查实验所属用户 -> 返回结果或403。】

### 5.4 结构上传与前端可视化实现

结构上传接口位于`routers/upload.py`。用户上传PDB文件后，后端检查文件扩展名、文件大小和内容有效性，然后解析链信息和残基序列，返回`chains`和`pdb_content`。前端接收PDB内容后保存到Zustand状态，并传给MolstarViewer组件渲染三维结构。

MolstarViewer负责加载PDB结构并进行高亮显示。系统区分三类高亮：用户选中的靶标范围、预测或指定的热点残基、鼠标悬停残基。靶标范围和热点残基属于持久高亮，重置相机时不会被清除；hover属于临时反馈。SequenceViewer负责按链显示序列，支持拖拽选择残基范围，并在预测热点位置显示红点。用户双击红点可移除某个预测热点，点击“指定热点”后才将候选热点写入RFD3配置。

【图5-2 结构上传与Molstar展示截图：需要插入实际系统截图，包含上传后的目标蛋白三维结构、链信息和序列视图。】

【图5-3 热点残基红点标注截图：需要插入预测热点在序列下方显示红点、Molstar中热点残基高亮的截图。】

### 5.5 热点残基预测模块实现

热点预测后端服务由`HotspotPredictionService`和`HotspotModelAdapter`封装。前端调用`POST /api/predict-hotspot`并传入PDB内容，后端使用热点预测器输出热点列表。接口返回字段包括chain、residue、residue_name、score、label、num_hotspots、method、model_loaded和total_residues。

模型层采用热点预测初稿中的ESM-2 + GAT方案。数据处理时将实验热点标记为1，实验非热点标记为0，未知残基标记为-1并在训练和评估中mask。模型训练采用五折交叉验证，并使用PR-AUC作为重要选模指标。由于独立测试集中正样本比例较低，本文将该模块定位为候选排序工具，而非最终实验判定工具。

系统前端不会自动把预测结果写入正式热点配置，而是先以“候选热点”形式显示。该设计的原因是热点模型仍可能产生假阳性，需要用户结合结构上下文进行确认。只有用户点击“指定热点”后，系统才将候选热点写入`rfd3Config.hotspots`。

### 5.6 后端Pipeline与并行调度实现

后端完整pipeline由`DesignPipelineService.run_pipeline`实现。该方法接收PDB内容、热点残基、Binder长度、靶标范围、长度上下限、diffusion_batch_size、n_batches、任务名和文件名等参数。

第一步，系统构建RFD3配置并调用`experiment_service.create_pipeline_experiment`创建实验记录。第二步，系统调用`rfd3_adapter.run`生成backbone，并保存RFD3结果。第三步，系统调用`_extract_backbone_tasks`从RFD3结果中提取所有backbone任务。如果RFD3结果中没有`designs`列表，则回退使用`first_backbone_pdb`，以兼容旧格式。第四步，系统调用`scheduler.run_mpnn_tasks`并行执行MPNN任务，每个任务由`_run_mpnn_task`处理。第五步，系统将所有MPNN成功任务中的sequences展开为RF3工作项，并调用`scheduler.run_rf3_tasks`并行执行RF3验证。第六步，系统通过`_build_parallel_candidate_designs`聚合候选结果。

候选排序逻辑如下。

```text
排序键 = (
    -pLDDT,      # pLDDT越高越靠前
     RMSD,       # RMSD越低越靠前
    -ranking_score
)
```

如果RF3验证成功，候选的pLDDT和ranking_score优先采用RF3结果；如果RF3失败，则保留候选序列并标记`validation_status="failed"`，pLDDT可回退到RFD3 backbone结果。这样系统在部分子任务失败时仍能保留成功候选，而不是直接丢弃整个实验。

调度器`PipelineTaskScheduler`使用`ThreadPoolExecutor`实现受控并发。其构造参数包括`mpnn_slots`和`rf3_slots`，对应环境变量`DEEPBINDER_PIPELINE_MPNN_SLOTS`和`DEEPBINDER_PIPELINE_RF3_SLOTS`。在默认情况下二者均为1，保证单GPU环境安全；在多GPU环境中可调整为更高值。

【图5-4 后端并行Pipeline实现流程图：建议标注`DesignPipelineService`、`PipelineTaskScheduler`、`RFD3Adapter`、`MPNNAdapter`、`RF3Adapter`。】

【代码片段5-1 Pipeline核心伪代码：】

```python
backbones = extract_backbones(rfd3_result)
mpnn_tasks = scheduler.run_mpnn_tasks(
    backbones,
    lambda backbone: run_mpnn_task(backbone)
)
rf3_items = build_rf3_work_items(mpnn_tasks)
rf3_tasks = scheduler.run_rf3_tasks(
    rf3_items,
    lambda item: run_rf3_task(item)
)
candidates = build_parallel_candidate_designs(mpnn_tasks, rf3_tasks)
save_designs(experiment_id, candidates)
```

### 5.7 实验记录与结果导出实现

实验记录模块主要由`routers/experiments.py`、`services/experiments.py`、`repositories/experiments.py`和`services/experiment_archive.py`实现。用户完成pipeline后，系统将RFD3、MPNN和RF3结果保存到`experiments`表，将候选Binder保存到`experiment_designs`表。

实验列表接口支持分页、状态筛选、关键词筛选和管理员按用户筛选。实验详情接口返回实验配置、三步模型结果和候选设计列表。导出接口可以生成JSON报告，候选CSV接口可以导出候选设计的关键指标。实验详情页展示基本信息、RFD3结果、MPNN结果、RF3结果和候选设计表格，并对pLDDT来源、ranking来源和验证状态进行说明。

【图5-5 实验记录列表截图：插入实验名称、状态、创建时间、设计数量等字段的页面截图。】

【图5-6 实验详情与候选结果截图：插入候选设计卡片或表格，需显示pLDDT、RMSD、ranking_score和验证状态。】

### 5.8 前端工作台实现

新建设计页面`NewDesignPage.tsx`是前端核心页面。该页面整合文件上传、热点预测、靶标选择、RFD3参数配置、后端pipeline运行和结果展示。前端状态集中保存在`useAppStore.ts`中，包括PDB内容、链信息、选中范围、预测热点、RFD3配置、当前实验ID、RFD3结果、MPNN结果、RF3结果和运行状态。

用户点击“新建任务”时，前端调用`startProteinRfd3Run`。该方法不再调用单独的`runRFD3`接口，而是以`async_mode=true`调用`runPipeline`，由后端创建作业并运行完整流程。前端先轮询作业状态，若RFD3阶段性结果已经生成，则立即展示backbone预览；随后继续在后台轮询MPNN和RF3结果。后端返回的`mpnn_results`和`rf3_results`可能是聚合格式，前端通过`normalizePipelineMpnnResults`和`normalizePipelineRf3Results`进行兼容处理，将汇总结果转换为页面可展示的序列列表和最佳RF3预览结果。

前端手动“送入MPNN”和“送入RF3验证”按钮仍然保留，用于展示设计过程。它们调用对应接口时设置`preview_only: true`，不传正式实验ID，因此只更新前端展示状态，不会覆盖后端实验记录。这一实现满足“展示设计过程但不影响后端数据”的需求。

【图5-7 新建设计完整工作台截图：插入上传目标结构、RFD3参数、运行按钮和结果区域的完整页面截图。】

### 5.9 构建与部署实现

前端使用Vite构建，生产构建输出到`frontend/dist`。由于Molstar依赖体积较大，系统在`vite.config.ts`中将Molstar依赖单独拆分到`molstar` chunk，并设置合理的chunk大小警告阈值，避免构建时出现Molstar循环chunk警告和无意义的大chunk提示。系统还修复了Tailwind扫描正则导致的CSS minify warning。

后端部署使用Uvicorn运行FastAPI服务，可通过Nginx将`/api`请求反向代理到后端端口，并由Nginx服务前端静态文件。云服务器部署时需要配置JWT密钥、CORS白名单、登录限流和模型运行环境变量。

【图5-8 部署结构图：浏览器 -> Nginx -> frontend/dist静态文件；/api -> Uvicorn/FastAPI -> SQLite/模型服务。】

### 5.10 本章小结

本章重点介绍了系统实现。后端通过FastAPI分层结构实现认证、上传、设计、实验、监控和用户管理；前端通过React、Zustand和Molstar实现新建设计工作台和结果展示；模型调用通过Adapter封装；完整pipeline由后端统一执行，并通过调度器实现MPNN和RF3阶段的可控并行。相比单纯的算法脚本，DeepBinder体现了完整软件系统的设计和实现过程。

---

## 第6章 系统测试

### 6.1 测试方案

DeepBinder测试分为模型实验测试和软件系统测试两部分。模型实验测试用于验证热点预测模块是否具有候选排序能力；软件系统测试用于验证Web应用、接口服务、权限控制、pipeline编排和构建部署是否满足需求。

系统测试采用自动化测试为主、人工界面截图为辅的方式。后端使用pytest覆盖服务编排、路由契约、安全鲁棒性、模型适配和系统验收；前端使用Vitest和Testing Library覆盖页面流程；生产构建使用`npm run build`验证资源打包。测试命令如下。

```bash
.venv/bin/pytest binder-design-system/backend/tests/test_design_pipeline_service.py \
  binder-design-system/backend/tests/test_design_router_contract.py \
  binder-design-system/backend/tests/test_deepbinder_react_contract.py \
  binder-design-system/backend/tests/test_settings.py -q

cd binder-design-system/frontend
npm test -- --run src/pages/__tests__/acceptance.test.tsx
npm run build
```

【图6-1 测试执行截图：建议插入终端中pytest、Vitest和npm build通过的截图。】

### 6.2 热点预测模型实验

热点预测模块的数据和实验结果参考`热点残基预测论文初稿.md`。实验采用训练验证集五折交叉验证和独立测试集评估。独立测试集中用于评估的残基总数为10856，其中热点残基191个，负样本10665个，正样本比例约1.76%。由于类别极度不平衡，本文不把Accuracy作为主要指标，而关注ROC-AUC、PR-AUC和Top-K候选推荐能力。

表6-1为不同负采样比例五折交叉验证结果。

| 配置 | ROC-AUC | PR-AUC | Precision | Recall | Specificity | F1 | MCC |
|---|---:|---:|---:|---:|---:|---:|---:|
| neg1 | 0.7026 ± 0.0675 | 0.5473 ± 0.0919 | 0.4730 ± 0.1946 | 0.7168 ± 0.1741 | 0.5486 ± 0.3238 | 0.5333 ± 0.1426 | 0.2521 ± 0.1934 |
| neg2 | 0.7306 ± 0.0568 | 0.5812 ± 0.1134 | 0.5853 ± 0.2876 | 0.5014 ± 0.4248 | 0.6293 ± 0.4171 | 0.3665 ± 0.2525 | 0.1801 ± 0.1170 |
| neg3/final | 0.7051 ± 0.0806 | 0.5564 ± 0.1336 | 0.5756 ± 0.2974 | 0.5702 ± 0.3546 | 0.6367 ± 0.3829 | 0.4291 ± 0.2415 | 0.2235 ± 0.1916 |
| neg4 | 0.7310 ± 0.0266 | 0.5824 ± 0.1490 | 0.5021 ± 0.3780 | 0.1109 ± 0.0954 | 0.9519 ± 0.0493 | 0.1720 ± 0.1347 | 0.1123 ± 0.0994 |
| ESM2-MLP | 0.7947 ± 0.0471 | 0.0867 ± 0.0151 | 0.1541 ± 0.0710 | 0.1228 ± 0.0721 | 0.9810 ± 0.0155 | 0.1086 ± 0.0361 | 0.1047 ± 0.0225 |

表6-2为独立测试集二分类结果。

| 方法 | 阈值 | ROC-AUC | PR-AUC | Precision | Recall | Specificity | F1 | MCC |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Rule-based | 0.500 | 0.5597 | 0.0252 | 0.0315 | 0.1518 | 0.9164 | 0.0522 | 0.0322 |
| ESM2-MLP | 0.500 | 0.7431 | 0.1710 | 0.4571 | 0.2513 | 0.9947 | 0.3243 | 0.3304 |
| ESM2-GAT final | 0.575 | 0.7093 | 0.0886 | 0.0452 | 0.4974 | 0.8117 | 0.0828 | 0.1028 |

从表6-2可以看出，ESM2-GAT final的ROC-AUC为0.7093，PR-AUC为0.0886。由于测试集正样本比例仅约1.76%，随机PR-AUC期望约为0.0176，因此GAT模型仍具有一定正样本排序能力。但其Precision和F1不高，说明该模型不适合作为高置信二分类器，而更适合作为候选热点排序工具。

表6-3为Top-K候选推荐结果。

| 方法 | Top-3命中率 | Top-3召回 | Top-5命中率 | Top-5召回 | Top-10命中率 | Top-10召回 |
|---|---:|---:|---:|---:|---:|---:|
| Random | 0.0747 | 0.0250 | 0.1208 | 0.0428 | 0.2149 | 0.0858 |
| Rule-based | 0.1833 | 0.0733 | 0.2167 | 0.0838 | 0.3833 | 0.1571 |
| ESM2-MLP | 0.3500 | 0.2094 | 0.4500 | 0.3141 | 0.4833 | 0.3665 |
| ESM2-GAT final | 0.3833 | 0.1728 | 0.4333 | 0.2513 | 0.4833 | 0.3246 |

Top-K结果显示，ESM2-GAT final在Top-3命中率上达到38.33%，高于随机基线和规则基线，也略高于ESM2-MLP。Top-5和Top-10结果中，ESM2-GAT与ESM2-MLP接近。该结果支持系统将热点预测作为候选推荐模块，而不是完全自动决策模块。

【图6-2 ROC曲线：插入`hotspot-prediction/results/final_neg3/test_roc_curve.png`。】

【图6-3 PR曲线：插入`hotspot-prediction/results/final_neg3/test_pr_curve.png`。】

【图6-4 Top-K命中率对比柱状图：根据表6-3绘制Random、Rule-based、ESM2-MLP、ESM2-GAT在Top-3/5/10上的命中率。】

### 6.3 后端服务测试

后端测试覆盖服务编排、设计路由契约、设置读取和安全鲁棒性。与本文核心实现相关的测试包括以下内容。

（1）`test_design_pipeline_service.py`验证后端pipeline能够执行RFD3、MPNN和RF3完整流程；验证多个backbone会触发多次MPNN；验证多条sequence会触发多次RF3；验证MPNN槽位限制生效；验证某个RF3子任务失败时系统仍保留成功候选并记录失败候选状态。

（2）`test_design_router_contract.py`验证`/run-pipeline`请求契约、RFD3参数接收、预览式MPNN/RF3不持久化正式实验数据、RF3验证结果能够更新候选指标，以及设计结果字段来源正确。

（3）`test_security_robustness.py`验证匿名访问限制、对象级权限、管理员监控权限、注册输入校验、伪造Token拒绝、登录失败限流、上传文件鲁棒性和CORS白名单配置。

（4）`test_security_robustness.py`还验证生产环境不会创建固定默认管理员、管理员引导环境变量必须成对配置，以及GPU监控在`torch.cuda`不可用时可回退到`nvidia-smi`解析设备名称、显存和利用率。

（5）`test_settings.py`验证运行时配置能够从环境变量读取，包括`DEEPBINDER_ENV`、`DEEPBINDER_ALLOW_MOCK`、`DEEPBINDER_PIPELINE_MPNN_SLOTS`和`DEEPBINDER_PIPELINE_RF3_SLOTS`。

表6-4为核心后端测试结果。

| 测试范围 | 测试文件 | 结果 |
|---|---|---|
| Pipeline服务编排 | test_design_pipeline_service.py | 通过 |
| 设计路由契约 | test_design_router_contract.py | 通过 |
| React源码契约 | test_deepbinder_react_contract.py | 通过 |
| 配置读取 | test_settings.py | 通过 |
| 安全鲁棒性 | test_security_robustness.py | 通过（项目已有验收文档记录） |

最近一次核心验证命令结果为：后端核心pipeline、路由契约和安全鲁棒性相关测试均通过，仅存在Python依赖的deprecation warnings，不影响功能。

【补充数据6-1：如答辩材料需要，可在此补充完整`pytest`终端输出截图。】

### 6.4 前端验收测试

前端使用Vitest和Testing Library测试主要页面流程。验收测试覆盖匿名用户登录弹窗、研究人员登录、管理员监控入口、De Novo流程、实验记录列表、帮助页角色说明、蛋白到蛋白pipeline运行期间页面切换、异步pipeline轮询、热点标记保持、裁剪靶点固定和上传结构跨页面保持等场景。

最近一次前端验收测试命令为：

```bash
npm test -- --run src/pages/__tests__/acceptance.test.tsx
```

测试结果为23个测试全部通过。测试中存在一个预期的401登录失效日志，用于验证实验详情接口返回401时前端显示登录提示，不属于失败。

表6-5列出部分前端验收用例。

| 编号 | 用例 | 预期结果 | 结果 |
|---|---|---|---|
| FE-001 | 匿名访问应用 | 显示登录弹窗，设计页仍可见 | 通过 |
| FE-002 | 研究人员登录 | 显示研究员身份，不显示系统监控 | 通过 |
| FE-003 | 管理员登录 | 显示系统监控入口 | 通过 |
| FE-004 | De Novo手动流程 | RFD3、MPNN、RF3结果按预期展示 | 通过 |
| FE-005 | 异步后端pipeline运行中切换页面 | 通过作业轮询保留运行状态并展示完成结果 | 通过 |
| FE-006 | 裁剪靶点后继续拖拽序列 | 已固定靶标不被新的拖拽范围覆盖 | 通过 |
| FE-007 | 实验记录列表 | 正确展示实验名称、状态和设计数 | 通过 |

【图6-5 前端验收测试终端截图：插入Vitest 23 passed截图。】

### 6.5 构建验证与Warning修复

前端生产构建使用：

```bash
npm run build
```

最近一次构建结果显示3019个模块转换完成，输出`index`和`molstar`相关CSS/JS资源，构建成功。系统针对构建过程中的两个问题进行了修复。第一，Molstar内部依赖较大且存在循环重导出提示，系统在Vite配置中将`node_modules/molstar`固定拆分到`molstar` chunk，避免Rollup产生Molstar循环chunk warning。第二，Tailwind曾将`replace(/[-:T]/g, '')`误识别为arbitrary utility并产生CSS minify warning，系统将该正则替换为等价字符串处理逻辑，消除了CSS警告。第三，Molstar chunk体积超过Vite默认500KB阈值属于预期情况，因此配置合理的chunkSizeWarningLimit，避免无意义警告干扰构建判断。

【图6-6 前端build成功截图：插入`npm run build`输出截图，需包含`built in`和资源大小信息。】

### 6.6 系统测试结论

测试结果表明，DeepBinder系统能够完成主要业务流程。后端能够执行完整Binder pipeline并对MPNN/RF3阶段进行受控并行；实验数据能够正确持久化；前端能够展示结构、热点、RFD3结果、MPNN序列和RF3验证结果；权限控制和上传鲁棒性符合基本安全要求。热点预测模块在Top-K候选推荐场景中具有一定价值，但不应被描述为高精度二分类器。总体而言，系统达到了本科软件工程毕业设计对系统实现、测试验证和工程完整性的要求。

---

## 第7章 总结与展望

### 7.1 工作总结

本文围绕“基于深度学习的蛋白质Binder生成系统设计与实现”完成了一个面向蛋白质Binder设计流程的Web系统DeepBinder。与单独训练热点预测模型不同，本文重点放在软件系统的完整实现上，从需求分析、总体架构、数据库设计、接口设计、前端交互、后端服务、模型适配、任务调度到系统测试形成了较完整的软件工程闭环。

主要工作如下。

第一，设计并实现了前后端分离的DeepBinder Web应用。前端基于React、TypeScript、Zustand和Molstar实现结构上传、序列视图、热点标注、RFD3参数配置和结果展示；后端基于FastAPI、SQLAlchemy、SQLite和JWT实现用户认证、实验管理、模型服务编排和权限控制。

第二，集成了热点预测、RFD3、MPNN和RF3模型能力。热点预测模块采用ESM-2嵌入和GAT模型进行候选热点排序；RFD3用于生成Binder backbone；MPNN用于序列设计；RF3用于结构验证和指标计算。

第三，实现了后端主导的完整Binder设计pipeline。系统不再依赖前端逐步处理正式数据，而由后端统一创建实验、运行RFD3、并行执行MPNN和RF3、聚合候选、保存结果和归档报告。前端手动MPNN/RF3流程被定位为preview-only展示过程，不会影响后端正式实验数据。

第四，实现了轻量级进程内任务调度。系统将RFD3输出的多个backbone拆分为MPNN任务，将MPNN输出的候选序列拆分为RF3任务，并通过槽位参数限制并发，为多GPU或多进程资源利用提供基础。

第五，完成了系统测试和构建验证。后端核心测试、前端验收测试和生产构建均通过。热点预测实验说明模型在Top-K候选推荐中具有应用价值，系统测试说明DeepBinder能够完成端到端设计流程。

### 7.2 不足之处

本文系统仍存在以下不足。

第一，热点预测数据规模有限。独立测试集中热点残基比例较低，模型Precision和F1有限，因此热点预测模块更适合作为候选推荐工具，不能替代实验验证。

第二，当前任务调度为进程内实现，适合单机部署，但不具备分布式队列的持久化和跨机器调度能力。如果系统部署到多用户高并发环境，需要引入Redis、Celery或Kubernetes Job等更成熟的任务基础设施。

第三，Binder设计结果尚未经过湿实验验证。系统可以生成并筛选候选Binder，但其真实结合能力仍需要SPR、BLI、蛋白表达纯化或结构实验进一步确认。

第四，当前系统数据库使用SQLite，适合本科毕设和单机部署场景。若后续用于生产或团队协作，需要迁移到PostgreSQL等更适合并发访问的数据库。

第五，系统对模型运行环境依赖较强。RFD3、MPNN和RF3可能需要不同依赖、GPU资源和模型权重，部署仍有一定复杂度。

### 7.3 未来展望

后续工作可从以下方向改进。

（1）增强任务队列。将当前`PipelineTaskScheduler`替换或扩展为Redis/Celery队列，增加任务持久化、失败重试、优先级和分布式执行能力。

（2）完善设计评分。除pLDDT、RMSD和ranking_score外，可加入界面接触面积、氢键数量、疏水接触、聚集倾向、免疫原性和表达可行性等指标。

（3）扩展数据库和报告功能。支持更多实验元数据、模型版本追踪、参数版本追踪、候选结果对比和自动生成PDF报告。

（4）提升热点预测模型。整合更多热点数据库和传统物理化学特征，引入更大规模蛋白质语言模型或几何深度学习模型，提高Top-K候选推荐稳定性。

（5）开展实验验证。选择若干高分候选Binder进行表达、纯化和结合实验，形成“计算设计—实验验证—模型迭代”的闭环。

（6）优化用户体验。增加批量任务、任务进度条、失败原因诊断、结构对比视图和多用户协作能力，使系统更接近真实科研工具。

### 7.4 本章小结

本文完成了一个基于深度学习的蛋白质Binder生成系统。系统以软件工程实现为核心，整合了前端可视化、后端服务、模型适配、实验管理和任务调度。虽然系统仍需要更多实验数据和湿实验验证，但其已经具备端到端Binder设计流程的工程基础，为后续蛋白质设计工具平台化提供了可扩展实现。

---

## 参考文献

[1] Clackson T, Wells J A. A hot spot of binding energy in a hormone-receptor interface[J]. Science, 1995, 267(5196): 383-386.

[2] Kortemme T, Baker D. A simple physical model for binding energy hot spots in protein-protein complexes[J]. Proceedings of the National Academy of Sciences, 2002, 99(22): 14116-14121.

[3] Guerois R, Nielsen J E, Serrano L. Predicting changes in the stability of proteins and protein complexes: a study of more than 1000 mutations[J]. Journal of Molecular Biology, 2002, 320(2): 369-387.

[4] Chen R, Liu Y, Li Z, et al. PPI-hotspotID: A method for detecting protein-protein interaction hot spots from the free protein structure[J]. eLife, 2020, 9: e55215.

[5] Rives A, Meier J, Sercu T, et al. Biological structure and function emerge from scaling unsupervised learning to 250 million protein sequences[J]. Proceedings of the National Academy of Sciences, 2021, 118(15): e2016239118.

[6] Lin Z, Akin H, Rao R, et al. Evolutionary-scale prediction of atomic-level protein structure with a language model[J]. Science, 2023, 379(6637): 1123-1130.

[7] Velickovic P, Cucurull G, Casanova A, et al. Graph Attention Networks[C]. International Conference on Learning Representations, 2018.

[8] Kipf T N, Welling M. Semi-Supervised Classification with Graph Convolutional Networks[C]. International Conference on Learning Representations, 2017.

[9] Watson J L, Juergens D, Bennett N R, et al. De novo design of protein structure and function with RFdiffusion[J]. Nature, 2023, 620: 1089-1100.

[10] Dauparas J, Anishchenko I, Bennett N, et al. Robust deep learning-based protein sequence design using ProteinMPNN[J]. Science, 2022, 378(6615): 49-56.

[11] Baek M, DiMaio F, Anishchenko I, et al. Accurate prediction of protein structures and interactions using a three-track neural network[J]. Science, 2021, 373(6557): 871-876.

[12] Jumper J, Evans R, Pritzel A, et al. Highly accurate protein structure prediction with AlphaFold[J]. Nature, 2021, 596: 583-589.

[13] Vaswani A, Shazeer N, Parmar N, et al. Attention is all you need[C]. Advances in Neural Information Processing Systems, 2017.

[14] Hu J, Shen L, Sun G. Squeeze-and-Excitation Networks[C]. IEEE Conference on Computer Vision and Pattern Recognition, 2018: 7132-7141.

[15] Lin T Y, Goyal P, Girshick R, et al. Focal Loss for Dense Object Detection[C]. IEEE International Conference on Computer Vision, 2017: 2980-2988.

[16] Berman H M, Westbrook J, Feng Z, et al. The Protein Data Bank[J]. Nucleic Acids Research, 2000, 28(1): 235-242.

[17] Cock P J A, Antao T, Chang J T, et al. Biopython: freely available Python tools for computational molecular biology and bioinformatics[J]. Bioinformatics, 2009, 25(11): 1422-1423.

[18] Wang M, Zheng D, Ye Z, et al. Deep Graph Library: A Graph-Centric, Highly-Performant Package for Graph Neural Networks[J]. arXiv preprint arXiv:1909.01315, 2019.

[19] Sehnal D, Bittrich S, Deshpande M, et al. Mol* Viewer: modern web app for 3D visualization and analysis of large biomolecular structures[J]. Nucleic Acids Research, 2021, 49(W1): W431-W437.

[20] Ramachandran P, Zoph B, Le Q V. Searching for activation functions[C]. International Conference on Learning Representations Workshop, 2018.

[21] Pedregosa F, Varoquaux G, Gramfort A, et al. Scikit-learn: Machine Learning in Python[J]. Journal of Machine Learning Research, 2011, 12: 2825-2830.

[22] Paszke A, Gross S, Massa F, et al. PyTorch: An imperative style, high-performance deep learning library[C]. Advances in Neural Information Processing Systems, 2019.

[23] Kluyver T, Ragan-Kelley B, Pérez F, et al. Jupyter Notebooks-a publishing format for reproducible computational workflows[C]. Positioning and Power in Academic Publishing: Players, Agents and Agendas, 2016.

[24] Ramírez-Aportela E, López-Blanco J R, Chacón P. FRODOCK 2.0: fast protein-protein docking server[J]. Bioinformatics, 2016, 32(15): 2386-2388.

[25] Fowler D M, Fields S. Deep mutational scanning: a new style of protein science[J]. Nature Methods, 2014, 11(8): 801-807.

---

## 致谢

在毕业设计和论文撰写过程中，得到了老师、同学和家人的帮助，在此表示衷心感谢。

首先，感谢指导老师在选题、系统设计、论文结构和实现细节方面给予的指导。老师对论文规范、工程完整性和测试验证的要求，使本文能够从单一模型实验扩展为较完整的软件系统实现。

其次，感谢本科阶段各位任课老师的教学。软件工程、数据库、Web开发、软件测试和人工智能相关课程为本课题的完成提供了基础。通过本次毕业设计，也进一步理解了需求分析、架构设计、编码实现、测试验证和部署维护之间的关系。

感谢开源社区提供的工具和框架。FastAPI、React、Molstar、PyTorch、DGL、ESM-2、RFD3、MPNN和RF3等开源项目为系统实现提供了重要支撑。

最后，感谢家人和朋友在毕业设计期间给予的理解和支持。正是这些帮助和鼓励，使本人能够持续完成系统开发、测试和论文整理工作。

---

## 附录

### 附录A 论文中需要补充的图片清单

| 编号 | 图片名称 | 建议来源 |
|---|---|---|
| 图1-1 | 课题应用背景示意图 | 自绘流程图 |
| 图2-1 | ESM-2 + GAT热点预测流程图 | 根据热点预测初稿自绘 |
| 图3-1 | 系统业务流程图 | 根据第3章业务流程自绘 |
| 图3-2 | 系统用例图 | draw.io/ProcessOn绘制 |
| 图4-1 | 系统总体架构图 | 根据第4章分层架构自绘 |
| 图4-2 | 系统功能模块图 | 自绘模块图 |
| 图4-3 | 后端Pipeline时序图 | 自绘时序图 |
| 图4-4 | MPNN/RF3任务拆分与槽位调度图 | 自绘并行调度图 |
| 图4-5 | 数据库E-R图 | 根据database.py绘制 |
| 图4-6 | 新建设计页面线框图 | 系统截图或自绘 |
| 图4-7 | 前端状态流转图 | 根据useAppStore绘制 |
| 图5-1 | 登录与权限校验时序图 | 自绘 |
| 图5-2 | 结构上传与Molstar展示截图 | 运行系统截图 |
| 图5-3 | 热点残基红点标注截图 | 运行系统截图 |
| 图5-4 | 后端并行Pipeline实现流程图 | 自绘 |
| 图5-5 | 实验记录列表截图 | 运行系统截图 |
| 图5-6 | 实验详情与候选结果截图 | 运行系统截图 |
| 图5-7 | 新建设计完整工作台截图 | 运行系统截图 |
| 图5-8 | 部署结构图 | 自绘 |
| 图6-1 | 测试执行截图 | 终端截图 |
| 图6-2 | ROC曲线 | `hotspot-prediction/results/final_neg3/test_roc_curve.png` |
| 图6-3 | PR曲线 | `hotspot-prediction/results/final_neg3/test_pr_curve.png` |
| 图6-4 | Top-K命中率柱状图 | 根据表6-3绘制 |
| 图6-5 | 前端验收测试终端截图 | 终端截图 |
| 图6-6 | 前端build成功截图 | 终端截图 |

### 附录B 论文中需要补充或核对的数据

1. 实际服务器硬件环境：CPU型号、GPU型号、显存、内存、操作系统版本。
2. 实际部署地址或本地运行端口截图。
3. 热点预测ROC/PR曲线图片路径是否存在，如不存在需从训练结果重新导出。
4. 系统实际运行一次pipeline的样例实验记录，包括任务名、RFD3生成数量、MPNN序列数量、RF3验证数量、最佳候选pLDDT/RMSD/ranking_score。
5. 前端页面截图需要使用最终版本系统截取，避免与论文描述不一致。
6. 若学校要求查重/AI率，建议在提交前补充个人开发过程描述、真实调试问题、具体测试截图和代码提交记录，使论文更贴合本人项目实践。

### 附录C 核心环境变量

| 环境变量 | 作用 | 默认值 |
|---|---|---|
| DEEPBINDER_ENV | 运行环境，生产环境设为production | development |
| DEEPBINDER_FOUNDRY_MODE | 模型运行模式 | auto |
| DEEPBINDER_ALLOW_MOCK | 是否允许mock回退，开发环境默认允许，生产环境默认关闭 | development为true，production为false |
| DEEPBINDER_MODEL_TIMEOUT_SECONDS | 模型超时时间 | 1800 |
| DEEPBINDER_PIPELINE_MPNN_SLOTS | MPNN并行槽位 | 1 |
| DEEPBINDER_PIPELINE_RF3_SLOTS | RF3并行槽位 | 1 |
| DEEPBINDER_BOOTSTRAP_ADMIN_USERNAME | 管理员引导用户名 | 未设置 |
| DEEPBINDER_BOOTSTRAP_ADMIN_PASSWORD | 管理员引导密码，生产环境需显式配置 | 未设置 |
| DEEPBINDER_BOOTSTRAP_ADMIN_EMAIL | 管理员引导邮箱 | admin@deepbinder.local |
| DEEPBINDER_CORS_ORIGINS | CORS白名单 | 部署时配置 |
| DEEPBINDER_JWT_SECRET | JWT密钥 | 生产环境必须替换 |

### 附录D 主要测试命令

```bash
# 后端核心测试
.venv/bin/pytest binder-design-system/backend/tests/test_design_pipeline_service.py \
  binder-design-system/backend/tests/test_design_router_contract.py \
  binder-design-system/backend/tests/test_deepbinder_react_contract.py \
  binder-design-system/backend/tests/test_settings.py -q

# 前端验收测试
cd binder-design-system/frontend
npm test -- --run src/pages/__tests__/acceptance.test.tsx

# 前端生产构建
npm run build
```
